# OnQue 트러블슈팅 기록

> **이 프로젝트 전용 파일.** `~/Project/TROUBLESHOOTING.md`(전역 누적 파일)와 별도로,
> 사용자 요청에 따라 OnQue 폴더 안에 독립적으로 관리한다.
> 문제 → 원인 → 해결 → 추후 관리 순서로, 포트폴리오·면접 자료로 바로 쓸 수 있게 작성한다.

---

## 작성 규칙

1. 문제를 해결한 직후에 쓴다.
2. 재현되지 않은 추측은 원인에 쓰지 않는다. 확인한 것과 추정한 것을 구분한다.
3. 실패한 시도도 남긴다.
4. 새 항목은 인덱스 표 맨 위(최신순), 본문도 맨 위에 이어 붙인다.
5. 로그·에러 메시지는 원문 그대로 코드블록에 넣는다.
6. 비밀값(키·토큰·비밀번호·실제 도메인/IP)은 `<REDACTED>`로 치환한다.

---

## 인덱스

| ID | 날짜 | 영역 | 문제 | 심각도 | 상태 |
|---|---|---|---|---|---|
| TS-029 | 2026-08-10 | FE/레이아웃 | 접힌 목록에 `max-h-[45%]`를 걸었는데 상한이 무효 — 조상 체인에 확정 높이가 없어 `%`가 `aside` 자신의 높이를 기준 삼았고, 그 `aside`가 목록 따라 무한정 자람. 목록이 짧으면 정상으로 보여 tsc·빌드·pytest 어느 것도 못 잡음 | High | 해결됨 (2026-08-11 근본 수정 — 당시 "지금은 안전하다"는 추론이 실측에서 틀린 것으로 드러남, 방아쇠는 목록이 아니라 긴 본문) |
| TS-028 | 2026-08-10 | BE/보안 | 새 읽기 경로(AI 비서 컨텍스트)가 그룹 격리의 **두 번째 단**인 채팅방 멤버십 필터를 통째로 우회 — 화면에서 안 보이는 비공개 방 약속을 비서가 읽어줌. 태스크별 리뷰 8회가 전부 `group_id`만 확인하고 통과시킴 | High | 해결됨 |
| TS-027 | 2026-08-09 | BE/API | 예외 핸들러를 등록해뒀는데 404·405만 봉투를 빠져나감 — FastAPI의 `HTTPException`이 Starlette 것의 **하위** 클래스라, 하위에 건 핸들러가 상위로 던져지는 예외를 못 잡음 | Medium | 해결됨 |
| TS-026 | 2026-08-07 | Infra/개발환경 | 워크트리에서 `npm run build`가 코드를 컴파일하기도 전에 죽음 — TS-023에서 만든 `node_modules` 심링크를 Turbopack이 "파일시스템 루트 밖"으로 판정 | Medium | 우회 (`--webpack`) |
| TS-025 | 2026-08-06 | 프로세스/테스트 | 테스트 이름과 본문이 어긋난 걸 바로잡는 과정에서, 원래 커버되던 케이스가 대체 없이 사라짐 — 스위트는 통과하고 커버리지 숫자도 늘어서 안 드러남 | Medium | 해결됨 |
| TS-024 | 2026-08-06 | 프로세스/테스트 | "태스크마다 전체 테스트 통과 후 커밋"이 2번 태스크에서 구조적으로 불가능 — 전역 admin을 없앤 순간, 나중 태스크가 삭제할 동작을 단언하는 기존 테스트 4개가 그때부터 6번 태스크까지 계속 빨간불 | Medium | 우회 (실패 허용 집합 고정) |
| TS-023 | 2026-08-06 | Infra/개발환경 | `git worktree`로 만든 작업 트리에서 테스트가 아예 안 돌아감 — venv·.env·node_modules가 전부 gitignore 대상이라 체크아웃에 딸려오지 않음 | Medium | 해결됨 (심링크) |
| TS-022 | 2026-08-06 | BE/보안 | 접근 제어를 새로 걸었더니 방 삭제 시 약속이 전원에게 영구히 숨는 회귀가 생기고, 그 수정안인 FK `ON DELETE SET NULL`은 SQLite가 강제하지 않아 테스트로 검증 불가 | High | 해결됨 |
| TS-021 | 2026-08-06 | BE/도메인 로직 | 상태 전이 검사가 "종료 상태에서 나가는 것"만 막아 확정 건너뛰기·되돌리기가 통과 — 테스트 124개가 전부 통과해도 안 드러남 | High | 해결됨 |
| TS-020 | 2026-08-06 | BE/테스트 | 그룹 격리 테스트가 검증 지점에 도달도 못하고 `TypeError`로 죽음 — 시드 관리자가 양쪽 그룹 멤버였고, 신규 사용자는 admin이 아니라 그룹 생성이 403 | Medium | 해결됨 |
| TS-019 | 2026-08-06 | BE/API | 중복 검사를 `SELECT` 후 `INSERT`로 하면 경합 시 `IntegrityError`가 예외 핸들러를 우회해 봉투 없는 500이 나가고, 프론트의 `error.code` 분기가 깨짐 | Medium | 예방됨 (리뷰에서 차단) |
| TS-018 | 2026-08-05 | Infra/DB | 접근 규칙을 좁히는 스키마 변경(`chat_room_members`)을 백필 없이 배포하면 전원이 모든 채팅방에서 잠길 뻔함 | High | 예방됨 (배포 전 백필) |
| TS-017 | 2026-08-05 | Infra/배포 | 새 엔드포인트가 404 — Render 반영 지연이었으나, 추측한 URL과 CDN 캐시본 때문에 두 번 잘못된 결론을 냄 | Medium | 해결됨 |
| TS-016 | 2026-08-05 | Infra/DB | 프로덕션 DB의 4개 테이블이 그룹 기능 이전 스키마에 머물러 할 일·일정·채팅·문서가 전부 실패 중이었으나, 프론트엔드가 오류를 삼켜 정상으로 보임 | High | 해결됨 |
| TS-015 | 2026-08-05 | BE/UX | 요약 리포트의 이모지가 프론트엔드를 아무리 고쳐도 사라지지 않음 — 출처가 백엔드 LLM 프롬프트였음 | Medium | 해결됨 |
| TS-014 | 2026-08-05 | Infra/Auth | 배포된 앱에서 로그인이 무한 대기 — Render 무료 티어 콜드스타트(54초) + 백엔드 구버전 배포 + `render.yaml`에 `JWT_SECRET` 누락이 겹침 | High | 해결됨 |
| TS-013 | 2026-08-05 | BE | pytest용 SQLite 인메모리 DB가 FastAPI 워커 스레드에서 `no such table` 에러 (StaticPool 누락) | Medium | 해결됨 |
| TS-012 | 2026-08-05 | Infra | 프로젝트 폴더 이동(`~/Desktop`→`~/Project`) 후 venv 셔뱅 경로가 깨져 `ModuleNotFoundError` | Medium | 해결됨 |
| TS-011 | 2026-08-04 | Infra | Vercel이 GitHub push에 자동 반응하지 않아 백엔드만 재배포되고 프론트는 구버전으로 남음 | Medium | 해결됨 |
| TS-010 | 2026-08-04 | 외부API | Gemini 무료 티어 일시 과부하(503)로 `@비서` 봇 답변이 폴백 메시지로 나감 | Low | 해결됨 (재시도로 해소) |
| TS-009 | 2026-08-04 | DB | SQLAlchemy가 `postgresql://` 스킴에서 기본으로 psycopg2를 찾는데 psycopg3만 설치되어 있어 `ModuleNotFoundError` | Medium | 해결됨 |
| TS-008 | 2026-08-04 | Infra | `.env`의 `DATABASE_URL` 키 뒤 공백 + 키 없는 값만 있는 빈 줄이 섞여 환경변수 인식 실패 | Medium | 해결됨 |
| TS-007 | 2026-08-04 | BE | `db.py`가 `.env`를 직접 로드하지 않아 import 순서에 따라 `DATABASE_URL`을 못 찾고 기동 실패 | Medium | 해결됨 |
| TS-006 | 2026-08-04 | FE | 이력 페이지가 SSR에서는 빈 상태, 클라이언트에서는 즉시 localStorage 값으로 렌더링돼 hydration mismatch 발생 | Medium | 해결됨 |
| TS-005 | 2026-08-04 | FE/Build | 서버 컴포넌트에서 함수를 클라이언트 컴포넌트 props로 전달해 Next.js 빌드가 `/calls`에서 실패 | High | 해결됨 |
| TS-004 | 2026-08-04 | Build/Infra | Next.js 16.0.3의 critical RCE(CVSS 10) 등 취약점으로 Vercel이 배포를 차단 | Critical | 해결됨 |
| TS-003 | 2026-08-04 | 외부API | Google이 신규 발급한 Auth key(`AQ.` 형식)가 구버전 `google-generativeai` SDK와 비호환 | High | 해결됨 |
| TS-002 | 2026-08-04 | Auth/Infra | `gemini_test.py`에 하드코딩된 실제 Gemini API 키가 첫 커밋으로 공개 저장소에 노출 (보안 사고) | Critical | 해결됨 (키 폐기·재발급, git 히스토리 정리는 사용자 판단으로 보류) |
| TS-001 | 2026-08-04 | Infra/Build | `requirements.txt`에 무관한 패키지 63개가 그대로 남아있어 Render pip 설치가 `ResolutionImpossible`로 실패 | High | 해결됨 |

**영역**: `FE` / `BE` / `DB` / `Infra` / `Build` / `Auth` / `외부API`
**심각도**: `Critical`(서비스 중단·데이터/보안 손실) / `High`(핵심 기능 불가) / `Medium`(우회 가능) / `Low`(불편)

---

## 기록

## TS-029 · `max-h-[45%]`가 상한 노릇을 못 했다 — 기준이 될 높이가 없었다

| | |
|---|---|
| **날짜** | 2026-08-10 |
| **영역** | FE / 레이아웃 (Tailwind · flexbox) |
| **심각도** | High (기능 전체를 못 쓰게 만듦. 자동 검증이 전부 통과) |
| **상태** | 해결됨 |
| **소요 시간** | 리뷰어가 브라우저 실측으로 특정, 수정은 한 줄 |

### 증상

우측 패널의 할 일·일정 목록을 접었다 펼치는 기능을 넣으면서, 펼침 영역에 `max-h-[45%]`를 걸어 화면의 절반 아래는 AI 비서가 쓰도록 했다.

**목록이 짧을 때는 완벽히 정상으로 보인다.** 그런데 목록이 길어지면 비서 입력창이 화면 밖으로 사라진다. 에러는 없다. 콘솔도 조용하다.

`npx tsc --noEmit` 0건, `npx next build --webpack` 성공, 백엔드 `pytest` 223 passed. **자동 검증 어느 것도 이걸 잡지 못한다.**

### 재현 조건

- 조상 체인에 확정 높이(`h-screen`/`h-full`)가 없고 `min-h-screen`만 있는 셸
- 그 안의 flex 컬럼 자식에 백분율 `max-height`
- **목록 항목이 충분히 많아야 한다.** 3개로는 재현되지 않는다

### 원인

**표면**: `max-h-[45%]`가 안 먹는다.

**근본**: `max-height`의 백분율은 **부모의 height가 확정(definite)일 때만** 해석된다. 조상 체인을 따라가 보니 확정 높이가 하나도 없었다.

```
app/layout.tsx의 <body>        높이 지정 없음
globals.css의 html/body        높이 지정 없음
AuthGuard.tsx: <div className="flex min-h-screen">   ← min-h-screen은 하한선이지 확정 높이가 아니다
  └── <aside className="flex flex-col overflow-hidden">  ← 자체 높이 클래스 없음
        └── <div className="max-h-[45%] shrink-0 overflow-y-auto">  ← 기준이 될 높이가 없다
```

여기서 함정이 하나 더 있다. **`max-h-[45%]`가 무시되고 끝나는 게 아니다.** flex의 2-pass 레이아웃에서 `aside`의 최종 높이가 정해진 뒤 그 45%로 계산되는데, **그 `aside` 높이 자체가 목록 길이를 따라 무한정 자란다.** 그래서 상한을 걸어도 상한선이 같이 커진다.

`aside`에 붙인 `overflow-hidden`도 아무것도 자르지 않는다 — 내용이 늘어난 만큼 `aside`도 같이 늘어나므로 넘칠 것이 없다. 대신 문서 전체가 스크롤 가능해지고, 비서 입력창은 뷰포트 훨씬 아래에 남는다.

리뷰어가 같은 Tailwind 클래스 구조를 복제해 Chrome에서 실측한 값 (뷰포트 852px):

| 목록 항목 수 | `aside` 높이 | 입력창 상태 |
|---|---|---|
| 3개 | 852px | 정상 |
| 60개 | **2088px** | 화면 밖 |
| 200개 | **6638px** | 완전히 밀림 |

### 시도했지만 안 된 것

- **`overflow-hidden`을 믿었다.** `aside`의 `overflow-y-auto`를 `overflow-hidden`으로 바꾸면 넘치는 내용이 잘릴 거라 봤다. 잘릴 내용이 애초에 생기지 않는다는 걸 놓쳤다.
- **`shrink-0`이 안전장치라고 봤다.** 오히려 반대였다 — 상한이 무효인 상태에서 `shrink-0`은 "줄어들지도 마라"는 뜻이라 문제를 굳혔다.
- **육안 확인.** 개발 중 목록은 항상 몇 개뿐이라 화면상 아무 문제가 없었다.

### 해결

`max-h-[45%]` → `max-h-[45vh]` **한 줄**.

`vh`는 조상의 확정 높이 여부와 무관하게 항상 뷰포트를 기준 삼는다. 펼침 영역이 스스로 캡핑되므로 `aside`가 무한정 자라는 원인 자체가 사라진다.

### 검증

같은 리프로를 수정 전/후로 대조 실측했다 (뷰포트 925px):

| 항목 수 | 수정 전 `aside` | 수정 후 `aside` | 수정 후 펼침 영역 | 입력창 |
|---|---|---|---|---|
| 3 | 925px | 925px | 97px | 보임 |
| 60 | 1978px | **925px** | **416.25px** (=925×0.45) | 보임 |
| 200 | 6458px | **925px** | **416.25px** | **보임** |

`tsc` 0건, `next build --webpack` 성공, 백엔드 223 passed 유지.

### 추후 관리

- **근본 원인은 남아 있다.** 셸(`AuthGuard.tsx`)에 확정 높이가 없다는 사실은 그대로다. 지금 안전한 건 `<main className="flex-1 overflow-y-auto">`가 컬럼을 늘리지 못하게 막고 있기 때문인데(flex 아이템의 자동 최소 크기는 `overflow`가 `visible`이 아니면 0), **이건 우연에 가까운 사실이다.** 누가 그 `overflow-y-auto`를 떼거나 `overflow: visible`인 형제를 추가하면 조용히 되살아난다.
- `min-h-screen` → `h-screen` 근본 수정은 사이드바·모바일 내비·모든 페이지의 스크롤 동작에 영향이 가고 **브라우저 실측이 필요하다.** 비서 브랜치의 회귀 위험과 묶지 않기로 하고 별도 작업으로 끊었다. 착수할 때 위 측정 절차를 그대로 재사용할 것.
- 위 CSS 분석 중 "지금은 안전하다"는 부분은 **명세 기반 추론이고 실측이 아니다.** 별도 작업에서 200개 항목 + 긴 본문 조합으로 한 번 재봐야 한다.

### 배운 점

**백분율 크기 속성을 쓸 때는 기준이 될 높이가 어디서 오는지 먼저 확인한다.** `min-h-*`는 확정 높이가 아니다. 조상 체인에 `h-screen`/`h-full`이 없으면 `%`는 뜻대로 동작하지 않고, **무시되는 게 아니라 엉뚱한 기준으로 계산되어 더 찾기 어렵다.** 뷰포트 기준이면 `vh`, 부모의 남은 공간이면 `flex-1 min-h-0`을 쓴다.

**타입 검사·빌드·단위 테스트가 전부 통과하는 결함 부류가 있다.** 레이아웃은 그중 대표다. 이번 건은 데이터 양에 따라서만 드러나서, "화면을 열어봤다"로도 안 잡혔다 — 개발용 데이터가 항상 적었기 때문이다. **레이아웃 상한을 걸었으면 데이터를 많이 넣고 재봐야 한다.** 리뷰어가 클래스 구조를 복제해 브라우저에서 수치를 잰 것이 이번 발견의 결정타였다.

### 후속 (2026-08-11) · 근본 수정 — 그리고 "지금은 안전하다"가 틀렸다

위 "추후 관리"에 남긴 두 문장을 이번에 검증했고, **하나는 틀렸다.**

> 지금 안전한 건 `<main className="flex-1 overflow-y-auto">`가 컬럼을 늘리지 못하게 막고 있기 때문인데(flex 아이템의 자동 최소 크기는 `overflow`가 `visible`이 아니면 0), 이건 우연에 가까운 사실이다.

**우연히 안전했던 게 아니라 애초에 안전하지 않았다.** 자동 최소 크기 규칙은 아이템이 *줄어들 수 있는 하한*만 0으로 만든다. 컬럼의 `height`가 `auto`인 이상 컬럼 높이는 자식의 **max-content 기여**로 정해지고, `flex-basis: 0`이어도 `<main>`의 max-content 기여는 본문 전체 높이다. 그래서 본문이 길면 컬럼이 그만큼 자라고 형제인 `aside`도 같이 늘어난다.

방아쇠가 **목록이 아니라 본문**이었기 때문에 TS-029의 `max-h-[45vh]` 수정과 무관하게 살아 있었다. 목록을 3개로 접어둬도 대시보드 본문만 길면 그대로 재현된다.

같은 리프로 하네스, 뷰포트 1055×908, 접힘 목록 200항목 + 본문 120단락:

| | `min-h-screen` (수정 전) | `lg:h-screen` (수정 후) |
|---|---|---|
| `aside` 높이 | **3284px** | 908px |
| 비서 입력창 bottom | 3272px (**화면 밖**) | 896px (보임) |
| 페이지 전체 스크롤 | 발생 | 없음 |
| `main` 자체 스크롤 | **안 함** (`overflow-y-auto`가 무의미) | 동작 |
| 사이드바 높이 / 클리핑 | 3284px / 없음 | 908px / 없음 |

접힘 목록만 늘린 조합(본문 짧음, 3·60·200항목)은 수정 전에도 전부 정상이었다 — TS-029의 `vh` 수정이 그쪽은 이미 막고 있었다.

### 시도했지만 안 된 것 (후속)

- **`h-screen`을 무조건 걸기.** 원래 계획이었고 lg 이상에서는 완벽했지만, 375px에서 **페이지 스크롤이 `main` 내부 스크롤로 바뀐다**(`pageScrolls` true→false, `mainScrolls` false→true). 모바일 브라우저의 `100vh`는 주소창을 포함해 실제 보이는 높이보다 크므로, 페이지가 안 스크롤되면 바닥이 주소창에 가린 채 남는다. 우측 패널은 `lg:flex`라 그 아래에는 애초에 밀려날 입력창이 없다 — 얻는 것 없이 위험만 지는 변경이었다.
- **`min-h-0` 추가.** 필요할 거라 봤는데 실측상 불필요했다. `main`이 이미 `overflow-y-auto`라 자동 최소 크기가 0이고, `aside` 안쪽 체인(`flex min-h-0 flex-1`)도 이미 갖춰져 있었다.

### 해결 (후속)

`AuthGuard.tsx`의 두 컨테이너에 `lg:h-screen`을 덧댔다. `min-h-screen`은 lg 미만을 위해 남긴다.

```diff
-<div className="flex min-h-screen">
+<div className="flex min-h-screen lg:h-screen">
   <Sidebar />
-  <div className="flex min-h-screen min-w-0 flex-1 flex-col">
+  <div className="flex min-h-screen min-w-0 flex-1 flex-col lg:h-screen">
```

브레이크포인트를 `lg`로 고른 이유는 **우측 패널이 `lg:flex`라 문제가 생길 수 있는 구간과 정확히 일치**하기 때문이다. 빌드된 CSS에서 `.lg\:h-screen{height:100vh}`이 `@media (min-width:64rem)` 안에 `.lg\:flex`와 나란히 들어간 것을 확인했다.

375 / 768 / 1023px에서는 수정 전과 **모든 측정값이 동일**하다.

### 남은 것 (후속)

- **lg 이상에서 `main`이 처음으로 실제 스크롤 컨테이너가 됐다.** `main`에 `tabindex`가 없어, 페이지를 열자마자 아무 데도 포커스하지 않은 채 `Space`/`PageDown`을 누르면 스크롤되지 않을 수 있다. 브라우저별 실측 필요.
- 뒤로가기 시 스크롤 복원. 브라우저 네이티브 복원은 `window.scrollY`만 되돌리고 내부 컨테이너의 `scrollTop`은 복원하지 않는다. 라우트 이동 시 초기화는 Next.js가 `scrollIntoView`로 처리해 정상임을 소스에서 확인했으나, 뒤로가기는 미검증이다.

### 배운 점 (후속)

**"지금은 안전하다"를 실측 없이 문서에 남기면 다음 사람이 그걸 근거로 쓴다.** 이번엔 그 문장에 "명세 기반 추론이고 실측이 아니다"라는 단서를 달아둔 덕에 다시 쟀고 틀린 게 드러났다. 단서가 없었으면 근본 수정을 더 미뤘을 것이다.

**flex 컨테이너의 높이가 `auto`면 자식의 `overflow`는 컨테이너 성장을 막지 못한다.** 자동 최소 크기(하한)와 max-content 기여(컨테이너 높이 결정)는 서로 다른 규칙이다. 막고 싶으면 컨테이너에 확정 높이를 줘야 한다.

**뷰포트 단위 수정은 데스크톱만 재면 절반만 잰 것이다.** 같은 `h-screen`이 데스크톱에서는 문제를 고치고 모바일에서는 새 문제를 만든다. 반응형 변형(`lg:`)으로 적용 구간을 문제 구간에 맞추는 편이 안전했다.

---

## TS-028 · 새 읽기 경로가 격리의 두 번째 단을 통째로 건너뛰었다

| | |
|---|---|
| **날짜** | 2026-08-10 |
| **영역** | BE / 보안 (접근 제어) |
| **심각도** | High (UI로 막아둔 정보가 다른 채널로 노출) |
| **상태** | 해결됨 |
| **소요 시간** | 태스크별 리뷰 8회가 놓침. 브랜치 전체 리뷰에서 발견 |

### 증상

AI 비서 기능을 추가했다. 비서는 사용자가 물으면 자기 그룹의 약속·할 일·일정을 근거로 답한다. 그룹 격리는 처음부터 최우선 항목이었고, 컨텍스트 수집 4개 컬렉터와 액션 검증 4개 kind **전부에 `group_id` 검사가 들어가 있었다.** 태스크마다 리뷰가 그걸 확인하고 통과시켰다.

그런데 같은 그룹의 A와 B가 있고 A만 초대된 비공개 채팅방이 있을 때:

- B가 **화면에서** 약속 목록을 보면 그 방에서 뽑힌 약속은 **안 보인다** (정상)
- B가 **비서에게** "우리 팀 약속 뭐 있어?"라고 물으면 그 약속의 내용·고객사·기한을 **그대로 읽어준다** (유출)

에러도, 경고도, 실패한 요청도 없다. 비서가 자기 일을 잘 하고 있을 뿐이다.

### 재현 조건

- 같은 그룹에 사용자 둘, 그중 하나만 들어간 채팅방
- 그 방의 대화에서 스윕이 약속을 추출해 저장 (`source_type="chat"`, `room_id` 채워짐)
- 방 비멤버가 비서에게 약속을 물음

### 원인

**표면**: `assistant_service._commitments()`가 `group_id`와 `status`로만 걸렀다.

**근본**: **이 저장소의 데이터 격리 경계는 `group_id` 하나가 아니라 `group_id` + 채팅방 멤버십 2단이다.** 그 사실이 코드 한 곳의 docstring에만 있었다.

```python
# routers/commitments.py — 이 규칙을 아는 유일한 곳이었다
def _visible_commitment_filter(user_id: int):
    """채팅방은 그룹 전원이 아니라 초대된 사람만 볼 수 있다.
    스윕은 그룹의 모든 방을 훑어 Commitment.evidence에 원문을 그대로
    저장하므로, 조회 시점에 방 멤버십으로 걸러야 비공개 방의 대화가
    그룹 전원에게 새지 않는다."""
```

기존 조회 경로(`GET /api/v1/commitments`, `PATCH`, `bulk-status`)는 전부 이 필터를 지나간다. **비서만 그걸 몰랐다.** `build_context`는 `user_id`를 인자로 받지도 않았고, `assistant_service.py` 전체에 `room`이라는 단어가 없었다.

설계 문서와 구현 플랜에도 `room`이 한 번도 등장하지 않는다. **구현 실수가 아니라 계획의 구멍이었다.**

액션 경로도 같았다. 안 보이는 약속의 상태 변경 카드가 만들어지고 **라벨과 payload에 그 약속의 내용·고객사가 렌더된 뒤**, 사용자가 승인을 눌렀을 때에야 기존 엔드포인트가 403을 던진다. 서버 실행은 막히지만 **내용 노출은 이미 끝난 뒤다.**

### 시도했지만 안 된 것 — 왜 8번의 리뷰가 못 잡았나

이건 "시도했지만 안 된 수정"이 아니라 **놓친 이유**의 기록이다. 그쪽이 더 값지다.

- **태스크별 리뷰 8회가 전부 "`group_id` 필터가 있는가"만 확인했다.** 있었다. 그래서 전부 통과했다. 격리 경계가 2단이라는 사실이 어느 태스크 브리프에도 없었으니, 리뷰어들은 자기가 받은 기준으로는 옳게 판정한 것이다.
- **"그룹 격리가 이 브랜치 최대 보안 위험"이라고 명시까지 해뒀다.** 위험을 지목한 것만으로는 부족했다 — **무엇이 격리 경계인지**를 같이 주지 않으면 지목이 헛돈다.
- 태스크 단위로 쪼갠 리뷰는 구조적으로 이걸 볼 수 없다. 각 태스크는 자기 파일만 본다. "기존 조회 경로는 필터를 하나 더 건다"는 사실은 **다른 파일에** 있었다.

### 해결

1. **가시성 규칙을 `commitment_service.py`로 승격했다.** `visible_commitment_filter`(목록용)와 `is_commitment_visible`(단건용) 두 함수. `routers/commitments.py`는 이제 그걸 import해 쓴다 — **규칙을 두 벌로 만들지 않기 위해서다.** 세 벌이 됐으면 다음번엔 한쪽만 고쳐진다.
2. `build_context(db, group_id, user_id)` — 약속 조회에 가시성 술어를 `and_`로 붙였다.
3. `validate_actions(db, group_id, raw_actions, user_id)` — 상태 변경 액션에서 가시성을 검사하고, 안 보이면 카드를 **만들지 않고** `dropped`로 집계한다. 노출이 렌더 전에 막힌다.
4. 설계 문서에 채팅방 멤버십 필터를 명시했다.

방이 삭제되면 `room_id`가 NULL이 되고 그때는 그룹 공개로 승격시키는 **기존 트레이드오프를 그대로 보존했다** — 검사할 멤버십이 더는 없으니 영구 은닉보다 낫다고 이전에 판단한 것이다(TS-022).

### 검증

테스트 2개를 추가했고, 둘 다 **양방향 가드**로 짰다:

- 비공개 방 약속이 비멤버의 `build_context` 결과와 `render_context` 평문 **양쪽**에서 빠지는가, **그리고 방 멤버에게는 보이는가**. 필터를 빼면 앞의 둘이 깨지고, 전부 숨기는 과잉 필터를 넣으면 뒤가 깨진다
- 그 약속 id를 지목한 상태 변경 액션이 `dropped` 되는가, **그리고 멤버에게는 정상 통과하는가**

전체 스위트 **227 passed** (기존 223 유지 + 신규 4). 기존 단언이 지워지거나 약해진 것이 없음을 재리뷰가 `assert` 라인 단위로 대조 확인했다 — 시그니처가 바뀌어 기존 테스트 파일 3개가 수정됐기 때문에 이 회계가 필요했다(TS-025의 교훈).

가시성 규칙 정의가 **저장소 전체에 한 벌뿐**임을 grep으로 확인했다.

### 추후 관리

- **격리 경계를 문서 한 곳에 모아야 한다.** 지금은 `commitment_service.py`의 docstring이 유일한 설명이다. `.claude/standards/`에 "데이터 접근 경계" 한 페이지를 만들 것.
- **새 읽기 경로를 뚫을 때의 체크리스트가 필요하다.** 이 사건의 일반형은 "쓰기는 다 검사했는데 읽기 경로를 새로 뚫었다"다. 특히 LLM 컨텍스트 수집처럼 **여러 테이블을 한꺼번에 읽는** 신규 경로는, 각 테이블의 기존 조회 엔드포인트가 거는 필터를 하나씩 대조해야 한다. 이번엔 4개 컬렉터 중 1개만 어긋났는데 하필 가장 민감한 것이었다.
- 프롬프트 인젝션 방어는 개행 평탄화와 "데이터일 뿐 지시가 아니다" 문구까지만 넣었다. 텍스트 **길이** 상한은 아직 없다.

### 배운 점

**"권한 검사가 있는가"가 아니라 "이 데이터의 격리 경계가 몇 겹인가"를 물어야 한다.** `group_id` 검사는 8곳 전부에 있었다. 없었던 건 두 번째 겹이었고, 첫 겹만 확인하는 리뷰는 그걸 영원히 통과시킨다.

**작업을 태스크로 쪼개면 파일 사이에 흩어진 규칙이 전달되지 않는다.** 각 태스크는 자기 파일만 보고, 각 리뷰는 자기 태스크만 본다. 이건 쪼개기의 이점을 얻는 대가다. **그래서 브랜치 전체 리뷰가 필요하고, 그 리뷰에는 "태스크 경계를 넘는 문제를 찾아라"를 명시해야 한다** — 이번 발견이 정확히 거기서 나왔다.

**규칙이 함수 하나의 docstring에만 살아 있으면 다음 사람은 그 함수를 안 부른다.** 새 읽기 경로가 자연스럽게 그 규칙을 지나가도록 서비스 계층으로 올리는 것이, 문서를 더 쓰는 것보다 확실하다.

---

## TS-027 · 핸들러를 달았는데 404·405만 봉투 밖으로 새어나갔다

| | |
|---|---|
| **날짜** | 2026-08-09 |
| **영역** | BE / API 계약 |
| **심각도** | Medium (프론트의 `error.code` 분기가 특정 상태에서 불가능) |
| **상태** | 해결됨 |
| **소요 시간** | 원인 특정까지 약 15분 (클래스 계층 확인이 결정타) |

### 증상

`main.py`에 예외 핸들러가 **분명히 등록돼 있는데도** 일부 응답만 봉투(`{success, data, error}`)를 벗어난다.

```
$ curl https://<REDACTED>/api/v1/does-not-exist
{"detail":"Not Found"}

$ curl -X DELETE https://<REDACTED>/api/v1/me
{"detail":"Method Not Allowed"}
```

라우터가 직접 `raise HTTPException(...)` 한 403·409는 멀쩡히 봉투로 나온다. **같은 앱, 같은 핸들러인데 어떤 건 잡히고 어떤 건 안 잡힌다.** 에러 메시지에는 단서가 전혀 없다 — 핸들러가 안 걸렸다는 신호 자체가 없고, 그냥 FastAPI 기본 응답이 나올 뿐이다.

### 재현 조건

- FastAPI + Starlette
- 핸들러를 `@app.exception_handler(fastapi.exceptions.HTTPException)`으로 등록
- 존재하지 않는 경로(404) 또는 허용되지 않은 메서드(405) 요청

### 원인

**표면**: 404·405는 라우터가 아니라 **Starlette 내부**가 던진다.

**근본**: `fastapi.HTTPException`은 `starlette.exceptions.HTTPException`의 **하위 클래스**다. 핸들러를 하위 클래스에 걸면 상위 클래스로 던져진 예외는 매칭되지 않는다.

```
starlette.exceptions.HTTPException   ← Starlette 라우터가 404·405를 이걸로 던진다
        └── fastapi.HTTPException     ← 우리가 핸들러를 여기 걸어놨다
```

우리 라우터 코드는 항상 `fastapi.HTTPException`을 쓰니 전부 잡힌다. 그래서 **직접 짠 경로는 100% 정상으로 보이고, 프레임워크가 만드는 응답만 조용히 새어나간다.** 개발 중엔 라우터 코드만 테스트하니 드러날 일이 없다.

422(`RequestValidationError`)와 처리 못 한 예외(500)는 아예 별개 클래스라 애초에 핸들러가 없었다.

### 시도했지만 안 된 것

- **핸들러 본문을 의심** — `isinstance(exc.detail, dict)` 분기가 잘못됐나 먼저 봤다. 본문은 멀쩡했고, 애초에 **호출조차 안 되고 있었다.** 핸들러에 로그를 하나 심어봤다면 5분 만에 갈렸을 문제다.
- **경로 오타·prefix 문제 의심** — 404가 나오니 라우팅 설정을 봤다. 405가 같이 새는 걸 보고서야 "라우팅이 아니라 예외 처리"로 방향을 틀었다.

### 해결

핸들러를 **상위 클래스**에 등록한다. 하위 클래스는 자동으로 함께 잡힌다.

```python
from starlette.exceptions import HTTPException as StarletteHTTPException

@app.exception_handler(StarletteHTTPException)
async def http_exception_handler(request, exc: StarletteHTTPException):
    ...
    return JSONResponse(
        status_code=exc.status_code,
        content={"success": False, "data": None, "error": error},
        headers=getattr(exc, "headers", None),  # 405의 Allow 헤더를 버리면 규격 위반
    )
```

문자열 `detail`에 붙일 기본 코드도 상태 기반으로 바꿨다. 이전엔 전부 `INTERNAL_ERROR`라 404와 500이 프론트에서 구분되지 않았다.

`RequestValidationError`(422)와 `Exception`(500) 핸들러도 새로 달았다. 500은 스택 트레이스와 내부 경로를 **응답에 싣지 않고** 서버 로그에만 남긴다.

### 검증

프로덕션(Render 배포 후)에서 직접 확인했다.

```
$ curl https://<REDACTED>/api/v1/does-not-exist
{"success":false,"data":null,"error":{"code":"NOT_FOUND","message":"Not Found"}}

$ curl -X DELETE https://<REDACTED>/api/v1/me
{"success":false,"data":null,"error":{"code":"METHOD_NOT_ALLOWED","message":"Method Not Allowed"}}

$ curl -X POST https://<REDACTED>/api/v1/auth/signup -d '{"email":"not-an-email","password":"short","name":"이름"}'
{"success":false,"data":null,"error":{"code":"VALIDATION_FAILED","message":"입력값을 확인해주세요",
 "details":[{"field":"email","code":"value_error"},{"field":"password","code":"string_too_short"}]}}
```

테스트 5개를 먼저 써서 전부 실패(RED)하는 걸 확인한 뒤 구현했다. 전체 스위트 195 passed(기존 190 + 5).

500 응답에 비밀값이 안 새는지는 일부러 `RuntimeError("psycopg2 연결 실패: host=10.0.0.5 password=hunter2")`를 던져 응답 본문에 `psycopg2`·`hunter2`·`Traceback`·`routers/auth.py`가 없음을 단언했다.

### 추후 관리

- **500 응답에는 CORS 헤더가 붙지 않는다.** 처리 못 한 예외는 Starlette의 `ServerErrorMiddleware`가 처리하는데, 이건 `CORSMiddleware`보다 **바깥**에 있다. 프론트(Vercel)와 백엔드(Render)가 다른 오리진이라, 브라우저는 이 봉투를 읽지 못하고 CORS 오류로 본다. **이 동작은 이번 변경 이전과 동일하므로 회귀는 아니다** — 다만 "500인데 프론트에 CORS 에러가 뜬다"는 신고가 오면 이걸 먼저 떠올릴 것. 제대로 고치려면 미들웨어 순서를 손대야 한다.
- 422 문구가 pydantic 원문에서 "입력값을 확인해주세요"로 통일됐다. 필드별 사유는 `error.details`에 있으나 **프론트가 아직 안 읽는다.** 회원가입 화면의 문구를 세밀하게 하려면 그쪽 작업이 별도로 필요하다.
- 새 예외 클래스를 도입할 때는 **클래스 계층을 먼저 확인**하고 가능한 한 상위에 핸들러를 건다.

### 배운 점

**핸들러가 "등록돼 있다"와 "호출된다"는 다른 말이다.** 등록은 성공했고 문법 오류도 없었지만 매칭이 안 됐다. 예외 핸들러가 안 먹는 것 같으면 본문을 읽기 전에 **로그를 한 줄 심어 호출 여부부터 가른다.** 본문 디버깅은 그다음이다.

**프레임워크가 대신 만들어주는 응답이 계약의 사각지대다.** 우리가 짠 코드는 전부 봉투를 지켰다. 깨진 건 아무도 안 짠 경로 — 없는 URL, 잘못된 메서드, 검증 실패, 예상 못 한 예외 — 였다. **직접 짠 것만 테스트하면 이 구멍은 영원히 안 보인다.** 그래서 이번엔 "라우터를 거치지 않는 응답"만 골라 테스트를 짰다.

---

## TS-026 · 개발환경을 살린 심링크가 빌드 도구를 죽였다

| | |
|---|---|
| **날짜** | 2026-08-07 |
| **영역** | Infra / 개발환경 |
| **심각도** | Medium (우회 가능, 프로덕션 무관) |
| **상태** | 우회 (`--webpack`) |
| **소요 시간** | 원인 특정까지 약 10분 |

### 증상

`git worktree`로 만든 작업 트리(`.claude/worktrees/group-scoped-roles`)에서 프론트엔드 빌드를 돌리면 컴파일을 시작하기도 전에 죽는다.

```
Error [TurbopackInternalError]: Symlink [project]/node_modules is invalid, it points out of the filesystem root
```

타입 에러도 아니고 코드 위치도 안 나온다. 방금 고친 파일 때문인지 환경 때문인지 메시지만으로는 구분이 안 된다.

### 재현 조건

- Next.js 16.3.0, 빌드 기본 번들러가 Turbopack
- `onque-frontend/node_modules`가 다른 경로(메인 체크아웃)로의 심링크
- 워크트리에서 `npm run build` 실행

### 원인

**표면**: Turbopack이 프로젝트 루트를 심링크 대상까지 따라가지 못하고, `node_modules`가 루트 밖을 가리킨다고 판정해 즉시 중단한다.

**근본**: TS-023의 해결책이 원인이다. 워크트리는 `.gitignore` 대상(venv, `.env`, `node_modules`)을 체크아웃에 딸려오게 하지 않으므로, 그때 메인 체크아웃 쪽으로 심링크를 걸어 개발환경을 살렸다. **pytest는 심링크를 그냥 따라가지만 Turbopack은 따라가지 않는다.** 한 도구를 위한 해결이 다른 도구의 전제를 깬 것이다.

내 변경 때문이 아니라는 건 `git stash`로 확인했다 — 변경을 걷어낸 원본 HEAD에서도 똑같이 재현된다.

### 시도했지만 안 된 것

- **에러 메시지만 보고 코드 문제로 접근** — "Symlink ... invalid"가 소스 파일 이름을 하나도 안 내놓는데도 처음엔 방금 만진 파일을 의심했다. 원본 HEAD에서 재현해 보기 전까지 방향이 틀려 있었다.
- **심링크를 실제 복사로 바꾸기는 하지 않았다.** `node_modules`를 복사하면 워크트리마다 수백 MB가 중복되고, TS-023이 심링크를 고른 이유가 그대로 사라진다.

### 해결

빌드 검증에 Turbopack을 우회한다.

```bash
cd onque-frontend && npx next build --webpack
```

webpack 경로는 심링크를 정상적으로 따라가고 타입 체크까지 완주한다. 진단용으로 `next.config.ts`에 `turbopack: { root: "/Users/tina/Project/OnQue" }`를 임시로 넣으면 Turbopack으로도 통과하지만, **이건 절대 커밋하면 안 된다** — 로컬 절대경로 하드코딩이라 머지되면 다른 환경에서 깨진다.

타입만 빠르게 보고 싶으면 번들러를 아예 건너뛴다.

```bash
cd onque-frontend && npx tsc --noEmit
```

### 검증

두 경로(`--webpack`, 임시 `turbopack.root`)가 **정확히 같은 5개 타입 에러**를 내는 것으로 우회가 검증을 약화시키지 않음을 확인했다.

```
✓ Compiled successfully in 29.1s
  Running TypeScript ...
app/announcements/page.tsx(13,25): error TS2339: Property 'role' does not exist on type 'AuthUser'.
components/Sidebar.tsx(168,95): error TS2339: Property 'role' does not exist on type 'AuthUser'.
Failed to type check.
```

작업 완료 후 같은 명령으로 에러 0건 + 빌드 성공을 확인했고, `git diff`로 `next.config.ts`가 원상 복구됐음을 확인했다.

### 추후 관리

- **워크트리에서 프론트 빌드를 검증할 때는 `npx next build --webpack`을 쓴다.** `npm run build`는 이 환경에서 항상 실패한다 — 코드 문제로 오인하지 말 것.
- 메인 체크아웃(`~/Project/OnQue`)에서는 `node_modules`가 실물이므로 `npm run build`가 정상 동작한다. 배포 전 최종 확인은 그쪽에서 하면 된다.
- `turbopack.root`를 커밋에 섞지 않았는지 매번 `git status`로 확인한다.

### 배운 점

**환경을 고치는 우회는 다른 도구의 전제를 깨는지까지 확인해야 한다.** TS-023에서 심링크가 pytest를 살리는 걸 확인하고 끝냈는데, 같은 워크트리에서 돌아야 할 도구가 하나 더 있었다. "테스트가 돈다"는 "개발환경이 복구됐다"와 같은 말이 아니다.

**빌드 도구의 에러가 소스 위치를 안 짚으면 코드가 아니라 환경을 먼저 의심한다.** 판별법은 싸다 — `git stash` 한 번이면 내 변경 유무와 무관하게 재현되는지 30초 만에 갈린다.

---

## TS-025 · 틀린 테스트를 고치다 맞는 테스트를 지웠다

| | |
|---|---|
| **날짜** | 2026-08-06 |
| **영역** | 프로세스 / 테스트 |
| **심각도** | Medium (보안 경계 커버리지 소실, 실제 취약점은 아님) |
| **상태** | 해결됨 |
| **소요 시간** | 리뷰 지적부터 재검증까지 약 25분 |

### 증상

그룹 라우터의 권한 검사를 `permissions.py` 헬퍼 호출로 바꾸는 작업(Task 3)에서, 구현자가 기존 테스트 두 개의 **셋업을 다시 썼다**고 보고했다.

```
concerns: rewrote test_member_cannot_add_member / test_member_cannot_remove_member setup
to actually add the rejected user as a non-admin group member first — those tests' names
implied that scenario but their original bodies tested a pure outsider instead
```

이 시점의 지표는 전부 좋았다.

```
4 failed, 165 passed   ← 실패는 허용 집합 4개와 정확히 일치, 통과는 160 → 165로 증가
```

회귀 없음, 통과 증가, 구현자가 자진 보고까지 함. 그냥 넘어갈 조건이 다 갖춰져 있었다.

### 재현 조건

권한 검사를 "엔드포인트별 인라인" 에서 "공통 헬퍼" 로 옮기면서, 헬퍼가 **거절 사유를 더 세분화**할 때 발생한다. 이 경우:

- 비멤버 → `require_group_member`가 먼저 걸러 `GROUP_ACCESS_FORBIDDEN`
- 멤버지만 관리자 아님 → `require_group_admin`이 걸러 `GROUP_MEMBER_ADD_FORBIDDEN`

기존 테스트는 이 둘을 구분하지 않았다. 이름은 `test_member_cannot_add_member`인데 본문은 외부인을 썼고, 기대 코드는 `GROUP_MEMBER_ADD_FORBIDDEN`이었다. 리팩터링 후 이 테스트는 **실패한다** — 결함 때문이 아니라, 코드가 이제 두 경우를 올바르게 구분하기 때문이다.

### 원인

**표면**: 외부인이 `POST /groups/{id}/members`·`DELETE /groups/{id}/members/{user_id}`를 호출하는 경우를 검증하는 테스트가 하나도 없게 됐다.

**근본**: 구현자의 판단 자체는 **옳았다.** 이름과 본문이 어긋난 테스트는 실제로 거짓말을 하고 있었고, 그걸 바로잡은 건 개선이다. 문제는 **바로잡는 방향이 한 쪽이었다는 것**이다.

원래 테스트는 이름은 "멤버", 본문은 "외부인"이었다. 여기서 갈 수 있는 길이 둘이다.

1. 본문을 이름에 맞춘다 → 멤버 케이스가 생기고 **외부인 케이스가 사라진다**
2. 이름을 본문에 맞춘다 → 외부인 케이스가 남고 멤버 케이스가 없다

정답은 **둘 다 만드는 것**이었는데, 1번을 골랐다. 그리고 1번은 통과 개수를 줄이지 않는다 — 테스트 하나가 다른 하나로 치환됐을 뿐이라 **어떤 지표에도 안 잡힌다.**

더 근본적으로: **권한 리팩터링에서 거절 경로가 1개에서 2개로 늘면, 테스트도 1개에서 2개가 돼야 한다.** 코드의 분기는 늘었는데 테스트는 그대로 1개면, 늘어난 분기 중 하나는 검증되지 않는다. 이걸 놓쳤다.

### 시도했지만 안 된 것

- **구현자 보고와 지표를 근거로 통과 처리** — 실제로 하마터면 그럴 뻔했다. 실패 집합 일치·통과 증가·자진 보고 세 가지가 다 초록불이었다. 이 건은 **어떤 자동 지표로도 안 잡힌다.** 리뷰어에게 "테스트 셋업을 다시 쓴 것은 커버리지가 조용히 좁아지는 전형적 경로다. 다시 쓴 테스트가 원래보다 더 증명하는가, 같은가, 덜 증명하는가를 판정하라"를 **명시적 과제로 지정**해서야 드러났다.
- **리뷰어가 diff만 보고 판단** — 안 된다. diff에는 "테스트가 바뀌었다"만 보인다. 사라진 케이스가 다른 파일에서 커버되는지는 diff 밖이다. 리뷰어가 전체 테스트 파일을 grep해 "완전 비멤버가 이 두 엔드포인트를 호출하는 테스트"가 없음을 확인하고서야 확정됐다.

### 해결

없어진 케이스를 되살리되, 고쳐진 케이스는 그대로 뒀다. 결과적으로 커버리지는 리팩터링 전보다 **늘었다.**

```python
def test_outsider_cannot_add_member(client):
    """이 그룹에 아예 속하지 않은 사람은 require_group_admin의 멤버십
    검사에서 먼저 걸러져 일반 GROUP_ACCESS_FORBIDDEN을 받는다."""
    ...
    assert res.status_code == 403
    assert res.json()["error"]["code"] == "GROUP_ACCESS_FORBIDDEN"
```

상태 코드만 단언하면 **다른 403도 통과**하므로 `error.code`까지 못 박았다. 두 경로가 둘 다 403이라, 코드를 안 보면 구분이 안 된다.

### 검증

```
venv/bin/pytest tests/test_group_routes.py -q
17 passed in 60.34s
```

- 새 테스트 2개가 진짜 비멤버로 셋업하는지(실수로 멤버가 되면 엉뚱한 이유로 통과한다) 별도 재리뷰에서 셋업 호출을 따라가 확인
- 다시 쓴 멤버 케이스 테스트 2개가 이번 수정에서 안 건드려졌는지 확인 → 멤버·외부인 양쪽 커버
- 수정 diff가 테스트 외 파일을 안 건드렸는지 확인 (`1 file changed, 46 insertions(+)`)

### 추후 관리

- 남은 태스크(4~8)에서도 권한 검사를 헬퍼로 옮긴다. 같은 패턴이 반복될 수 있으니 리뷰 지시문에 **"테스트 셋업을 다시 썼다면 원래보다 더/같음/덜 증명하는지 판정하라"** 를 계속 넣는다.
- 이번처럼 거절 사유가 세분화되는 지점에서는, 세분화된 만큼 테스트가 늘었는지 개수로 확인한다.

### 배운 점

**"테스트가 틀렸으니 고친다"는 순간이 커버리지가 가장 조용히 새는 지점이다.** 틀린 테스트를 고치는 건 명백히 옳은 일이라 저항이 없고, 통과 개수는 오히려 늘고, 어떤 CI 지표에도 안 잡힌다.

이번 건의 교훈은 구체적이다. **이름과 본문이 어긋난 테스트를 발견하면, 둘 중 하나로 맞추지 말고 둘 다 만든다.** 어긋났다는 건 원래 두 개였어야 할 것이 하나로 뭉개져 있었다는 뜻이다.

그리고 **리뷰어에게 무엇을 의심하라고 지정하지 않으면 안 나온다.** "코드를 리뷰하라"로는 이 건이 안 잡혔다. "다시 쓴 테스트가 원래보다 덜 증명하는지 판정하라"로 지정해야 잡힌다. 리뷰 품질은 리뷰어의 성능보다 **무엇을 보라고 했는지**에 더 좌우된다.

---

## TS-024 · 권한을 걷어내는 리팩터링에서는 "매 단계 전부 초록"이 성립하지 않는다

| | |
|---|---|
| **날짜** | 2026-08-06 |
| **영역** | 프로세스 / 테스트 전략 |
| **심각도** | Medium (진행은 가능하나 안전망이 약해짐) |
| **상태** | 우회 — 실패 허용 집합을 고정해 안전망 복원 |
| **소요 시간** | 판정까지 약 20분 (독립 재현 1회 + 리뷰어 교차검증 1회) |

### 증상

그룹별 권한 작업의 실행 플랜은 Global Constraints에 이렇게 적혀 있다.

> 태스크마다 `venv/bin/pytest tests/ -q` 전체를 돌려 모두 통과하는 것을 확인하고 커밋한다.

Task 1은 그대로 지켜졌다(162 통과). Task 2 구현자가 `DONE_WITH_CONCERNS`로 돌아왔다.

```
4 failed, 160 passed in 142.79s (0:02:22)

FAILED tests/test_announcement_routes.py::test_admin_can_post_announcement
FAILED tests/test_announcement_routes.py::test_anyone_can_list_announcements
FAILED tests/test_group_scoping.py::test_admin_can_create_company_wide_schedule_visible_in_every_group
FAILED tests/test_user_routes.py::test_admin_can_list_users
```

전부 같은 모양이다.

```
>       assert res.status_code == 200
E       assert 403 == 200
E        +  where 403 = <Response [403 Forbidden]>.status_code
```

### 재현 조건

Task 2 커밋(`4b1acbf`) 체크아웃 후 전체 스위트 실행. 100% 재현된다.

### 원인

**표면**: 네 테스트가 전역 관리자로 로그인해 200을 기대하는데 403이 온다.

**근본**: Task 2의 존재 이유가 바로 **전역 admin을 없애는 것**이다. `routers/auth.py`의 `role="admin" if is_first_user else "member"` 분기를 지웠으니, 이제 `User.role`이 `"admin"`인 사람은 아무도 없다. 그런데 아직 안 고친 세 파일이 여전히 그 값을 본다.

```
main.py:430/475/502       create_schedule / update_schedule / delete_schedule
routers/announcements.py:42   create_announcement
routers/users.py:22           list_users
```

이 세 곳은 각각 Task 4·5·6이 담당한다. 즉 **네 실패는 결함이 아니라, 플랜이 의도적으로 삭제하기로 한 동작을 아직 안 지운 상태의 정확한 반영**이다. `test_admin_can_list_users`는 Task 6에서 엔드포인트와 함께 파일째 삭제된다.

여기서 진짜 문제는 코드가 아니라 **플랜의 제약 자체**다. 권한 체계를 A에서 B로 옮기는 작업은 "A를 없애는 단계"와 "B를 다 까는 단계" 사이에 반드시 구멍이 생긴다. 그 구간에서 전부 초록을 요구하면, 구현자에게 남는 선택지는 두 개뿐이고 둘 다 나쁘다.

- 없앤 전역 admin을 임시로 되살린다 → 태스크의 목적을 되돌림
- 걸리적거리는 테스트를 지우거나 단언을 느슨하게 한다 → 안전망을 영구 손실

**설계 시점에 못 본 것**: 플랜을 쓸 때 태스크를 "독립적"이라고 전제했는데, 권한 리팩터링에서 **삭제는 원자적이지 않다.** 한 곳에서 권한 원천을 없애면 그걸 읽던 모든 곳이 동시에 무효가 되는데, 그 모든 곳을 한 태스크에 몰면 태스크가 너무 커진다.

### 시도했지만 안 된 것

- **"명시적으로 그룹을 만들도록 테스트를 고친다"를 이 넷에도 적용** — Task 2에서 다른 깨진 테스트들에는 통했던 방법이다. 여기선 안 된다. 이 엔드포인트들은 `group_id`를 아예 받지 않는다. `POST /schedules`는 Task 4에서야 `group_id`를 필수로 받게 되고, `GET /api/v1/users`는 그룹 개념이 없다. 그룹을 만들어줘도 판정에 쓰이지 않는다.
- **구현자 보고를 그대로 수용** — 하지 않았다. "나중 태스크가 고칠 것"은 진짜 회귀를 숨기기 딱 좋은 설명이다. 컨트롤러가 스위트를 독립 실행해 실패 4개를 직접 확인하고, 리뷰어에게 **"각 실패가 정말 전역 admin 제거 때문인지, 이번 diff의 결함 때문은 아닌지"** 를 별도 과제로 지정했다. 리뷰어가 세 파일이 이번 diffstat에 없다는 것까지 확인해 교차검증했다.

### 해결

제약을 "전부 통과"에서 **"정확히 이 4개만 실패"** 로 바꿨다. 목록을 원장(`progress.md`)에 노드 id 단위로 못 박고, 이후 모든 태스크의 지시문에 그대로 실었다.

```
## 실패 허용 집합 (Task 2~5 한정)
- tests/test_announcement_routes.py::test_admin_can_post_announcement      (Task 5가 해소)
- tests/test_announcement_routes.py::test_anyone_can_list_announcements    (Task 5가 해소)
- tests/test_group_scoping.py::test_admin_can_create_company_wide_schedule_visible_in_every_group (Task 4가 해소)
- tests/test_user_routes.py::test_admin_can_list_users                     (Task 6이 파일째 삭제)
Task 6 종료 시점에는 0 failed여야 한다.
```

핵심은 **"몇 개 실패"가 아니라 "어느 것이 실패"로 기준을 세운 것**이다. 개수만 세면 하나 고쳐지고 하나 깨져도 4로 같아서 안 드러난다. 노드 id로 고정하면 집합에서 벗어나는 순간 회귀다.

### 검증

- Task 2 종료: `4 failed, 160 passed` — 집합과 정확히 일치
- 리뷰어가 실패 4건을 개별 실행해 각각의 게이트 위치(`main.py:430/475/502`, `routers/announcements.py:42`, `routers/users.py:22`)를 지목하고, 세 파일 모두 이번 diffstat에 없음을 확인
- 이후 태스크 지시문에 "다섯 번째 실패는 네가 만든 회귀다. 최종 실패 목록을 그대로 보고하라"를 명시
- **최종 확인 기준**: Task 6 종료 시 `0 failed`. 이게 안 나오면 이 우회는 실패한 것이다

### 추후 관리

- Task 6이 끝나면 반드시 전체 스위트가 0 failed인지 확인한다. 허용 집합은 그 시점에 폐기한다.
- 허용 집합이 살아 있는 동안은 **회귀 탐지 감도가 떨어진 상태**다. 이 구간(Task 3~5)의 리뷰는 diff를 더 꼼꼼히 봐야 한다.
- 다음에 권한·인증 체계를 바꾸는 플랜을 쓸 때는, Global Constraints에 "전부 통과" 대신 **태스크별 기대 실패 목록**을 처음부터 적는다.

### 배운 점

**"모든 테스트 통과"는 기능을 더할 때의 기준이지, 걷어낼 때의 기준이 아니다.** 삭제형 리팩터링에서는 중간 상태가 반드시 빨갛고, 그걸 억지로 초록으로 만들려는 압력이 곧 테스트를 훼손하는 압력이 된다. 규칙이 사람을 나쁜 선택으로 몰면 규칙 쪽이 틀린 것이다.

그리고 **빨간 테스트를 허용하는 순간 "왜 빨간지"를 기계가 검증할 수 있는 형태로 적어야 한다.** "나중에 고쳐진다"는 문장은 검증 불가능하고, 진짜 회귀를 그 문장 뒤에 숨길 수 있다. 노드 id 목록은 검증 가능하다.

---

## TS-023 · gitignore가 개발환경 전체를 워크트리 밖에 남겼다

| | |
|---|---|
| **날짜** | 2026-08-06 |
| **영역** | Infra / 개발환경 |
| **심각도** | Medium (작업 시작 자체가 막힘, 우회 가능) |
| **상태** | 해결됨 |
| **소요 시간** | 약 10분 |

### 증상

그룹별 권한 작업(A묶음)을 `main`에서 격리하려고 워크트리를 만들었다.

```
git worktree add .claude/worktrees/group-scoped-roles -b feat/group-scoped-roles main
```

체크아웃은 98개 파일로 정상 완료됐다. 그런데 새 트리에서 플랜이 지시한 명령이 그대로는 하나도 안 돌아간다.

- `venv/bin/pytest tests/ -q` → 인터프리터가 없다
- `cd onque-frontend && npm run build` → `node_modules`가 없다
- 백엔드 기동 시 `DATABASE_URL`을 못 찾는다

### 재현 조건

`.gitignore`에 개발환경 디렉터리를 넣어둔 저장소에서 `git worktree add`를 하면 매번 재현된다. 이 저장소의 해당 항목:

```gitignore
.env
node_modules/
venv/
```

### 원인

**표면**: 새 워크트리에 venv·node_modules·.env가 없다.

**근본**: `git worktree`는 **추적 대상 파일만** 새 트리에 체크아웃한다. gitignore된 것은 정의상 추적 대상이 아니므로 따라오지 않는다. 그런데 이 프로젝트에서 gitignore된 것들이 곧 **실행에 필요한 전부**다 — 파이썬 인터프리터, 프런트 의존성, 실제 Gemini 키와 Neon 접속 문자열.

즉 워크트리는 "소스는 격리하되 환경은 공유해야" 쓸 수 있는데, git은 소스만 신경 쓴다. 워크트리를 처음 쓰는 저장소에서는 반드시 한 번 겪는다.

### 시도했지만 안 된 것

- **`venv`를 워크트리 안에 새로 만든다** — 63개 패키지 재설치라 느리고, 트리를 지울 때마다 반복된다. 무엇보다 `.env`(실제 키)는 복사하면 안 된다. 사본이 늘수록 유출 경로가 늘어난다.
- **절대경로로 루트 venv를 부른다** (`/Users/tina/Project/OnQue/venv/bin/pytest`) — 실제로 동작한다. cwd가 워크트리면 pytest의 rootdir도 워크트리라 워크트리 코드를 import한다. 하지만 플랜과 서브에이전트 지시문에 적힌 명령이 전부 `venv/bin/pytest` 상대경로라, 에이전트마다 경로를 바꿔 전달해야 한다. 실수 지점을 늘리는 선택이라 접었다.

### 해결

세 개를 루트 저장소로 심링크했다. 복사가 아니라 링크라 `.env` 사본이 생기지 않는다.

```bash
cd .claude/worktrees/group-scoped-roles
ln -s /Users/tina/Project/OnQue/venv venv
ln -s /Users/tina/Project/OnQue/.env .env
ln -s /Users/tina/Project/OnQue/onque-frontend/node_modules onque-frontend/node_modules
```

세 경로 모두 `.gitignore`에 이미 걸려 있어 심링크가 커밋 대상으로 잡히지 않는다.

### 검증

```
155 passed in 155.01s (0:02:35)
```

워크트리 루트에서 `venv/bin/pytest tests/ -q`로 실행. 플랜에 적힌 명령을 한 글자도 바꾸지 않고 통과했고, 이 숫자가 이후 태스크의 베이스라인이 됐다.

### 추후 관리

- 이 저장소에서 워크트리를 새로 만들 때마다 같은 심링크 3개가 필요하다. 반복되면 `scripts/setup_worktree.sh`로 뽑는다.
- **`node_modules` 심링크는 공유 자원이다.** 두 트리가 같은 실체를 본다. 브랜치 간 의존성 버전이 달라지면(`package.json` 변경) 한쪽 `npm install`이 다른 쪽을 조용히 바꾼다. 이번 작업은 의존성을 안 건드려서 안전했지만, 건드리는 브랜치라면 심링크 대신 별도 설치가 맞다.
- 워크트리를 지울 때 `git worktree remove`를 쓴다. `rm -rf`로 지우면 심링크는 따라 지워지지만(링크만 삭제, 대상은 무사) git 메타데이터가 남는다.

### 배운 점

**gitignore 목록은 "커밋하면 안 되는 것"의 목록이면서, 동시에 "새 체크아웃에 없는 것"의 목록이다.** 평소엔 앞의 의미만 보다가, 워크트리·CI 컨테이너·새 클론처럼 트리를 새로 만드는 순간 뒤의 의미가 튀어나온다. 이 저장소의 `.gitignore` 세 줄이 그대로 환경 구성 체크리스트였다.

부수적으로, 심링크가 복사보다 나은 이유가 속도만은 아니다. **`.env` 같은 비밀값은 사본 개수가 곧 유출 표면**이라, 링크로 두면 실체가 하나로 유지된다.

---

## TS-022 · 접근 제어를 FK 없는 포인터에 걸었더니, 고치려는 수정마저 테스트로 검증할 수 없었다

| | |
|---|---|
| **날짜** | 2026-08-06 |
| **영역** | BE / 보안 / 테스트 환경 |
| **심각도** | High (비공개 대화 유출 → 수정 과정에서 약속 은닉 회귀) |
| **상태** | 해결됨 |
| **소요 시간** | 발견부터 최종 검증까지 약 3시간 (수정 3라운드) |

### 증상

세 겹으로 겹친 건이라 순서대로 적는다.

**1층 — 유출.** 자율 약속 추적을 붙이면서, 채팅에서 추출한 약속의 근거(`evidence`)에 메시지 원문을 그대로 담고 그룹 전원에게 보여줬다. 그런데 이 앱의 채팅방은 그룹 공개가 아니다. 대표와 회계 둘만 있는 방에서 "C사 건 3천만원에 맞춰 시안 드릴게요"라고 하면, 그 방에 없는 팀원 대시보드에 그 문장이 인용구로 뜬다. 확정·무시 버튼까지 눌린다.

**2층 — 은닉 회귀.** 1층을 "조회 시 방 멤버십으로 거른다"로 고쳤다. 방을 찾는 경로가 `Commitment.source_id`(메시지 id) → `ChatMessage.room_id` → `ChatRoomMember`였다. `source_id`에는 FK가 없어서 방이 삭제되면 포인터가 끊기고, 검사는 안전하게 실패해 **아무에게도 안 보이게** 됐다. 방장 본인도 못 본다.

```
조회 → 약속 1건
DELETE /chat/rooms/{id} → 200
같은 방장이 다시 조회 → data: [], meta.total: 0
```

더 나쁜 건 아무도 삭제를 안 눌러도 벌어진다는 점이다. 마지막 멤버가 방을 나가면 방이 자동 삭제된다.

**3층 — 검증 불가.** 2층을 "`Commitment.room_id`를 직접 저장하고, 방 삭제 시 FK `ON DELETE SET NULL`로 비운다"로 고치려 했다. 구현자가 멈추고 재현 결과를 가져왔다 — **SQLite는 기본적으로 FK를 강제하지 않는다.** 이 저장소 어디에도 `PRAGMA foreign_keys=ON`이 없다. 프로덕션 Postgres에서는 동작하지만 테스트 155개는 SQLite로 돌아서, 요구한 회귀 테스트를 쓰면 실패한다.

### 재현 조건

1층: 비공개 방에 메시지 15개 이상 쌓고 스윕을 돌린 뒤, 그 방 비멤버로 `GET /api/v1/commitments`
2층: 위 상태에서 방을 삭제하고 **방장으로** 다시 조회
3층: SQLite 인메모리 엔진(`StaticPool`)에서 부모 행 삭제 후 자식 행의 FK 컬럼 확인 — NULL이 되지 않고 삭제된 id가 남는다

### 원인

**표면**: 가시성 판정이 FK 없는 포인터 역참조에 의존했다.

**근본**: `Commitment.source_id`는 원래 **출처 표시용**으로 설계됐다. "이 약속이 어디서 나왔는지" 보여주는 용도라 FK가 없어도 됐고, 끊겨도 링크 하나 안 되는 정도였다. 그런데 나중에 접근 제어가 그 위에 얹혔다. **표시용 데이터가 보안 판정의 근거가 되는 순간, 그 데이터의 무결성 요구 수준이 완전히 달라진다.** 설계 당시엔 없던 요구다.

3층의 근본은 다르다. **한 기능(FK 캐스케이드)을 두 DB 엔진 중 하나만 강제한다는 사실**이다. 개발·테스트는 SQLite, 운영은 Postgres인 구성에서 DB 계층 동작에 의존하면 "테스트는 통과하는데 운영에서만 다르게 도는" 코드가 된다. 이번엔 반대 방향이라 더 헷갈렸다 — 운영에서는 맞고 **테스트에서만** 안 됐다.

**확인된 사실**: `main.py:660` `create_chat_room`이 생성자를 `ChatRoomMember`로 등록한다. `main.py:686` `delete_chat_room`과 `main.py:800` `remove_room_member`(마지막 이탈 자동 삭제) 둘 다 같은 `_delete_room_cascade`를 부른다.

### 시도했지만 안 된 것

- **`source_id` 역참조를 유지한 채 방 삭제만 처리** — 메시지가 이미 지워진 뒤라 역참조할 대상이 없다. 삭제 시점에 뭘 해도 포인터는 복구되지 않는다.
- **FK `ON DELETE SET NULL`에 맡기기** — 3층 그대로. SQLite에서 안 돌아 테스트를 못 쓴다.
- **`PRAGMA foreign_keys=ON` 켜기** — 검토 후 반려. 기존 155개 테스트에 그동안 조용히 봐주던 FK 위반이 있을 수 있고, 그게 한꺼번에 터지면 이 브랜치와 무관한 수정에 끌려들어간다.
- **방 삭제 시 약속도 함께 삭제** — 확정해서 기한 추적 중이던 약속까지 날아간다. "약속을 놓치지 않는다"는 제품의 존재 이유와 어긋난다.
- **약속이 남은 방은 삭제를 막기** — 마지막 멤버 이탈 자동 삭제 경로에는 안내할 화면이 없다.

### 해결

세 층을 각각 다르게 처리했다.

**1층**: 조회 시 방 멤버십으로 필터. 목록·단건 변경·일괄 변경 **세 경로 전부**에 적용했다. 목록만 막으면 id를 직접 넣어 확정·무시할 수 있다. SQL에서 `limit`보다 **먼저** 걸어 페이지가 조용히 줄지 않게 했고, 숨긴 건과 남의 그룹 건의 403 응답을 본문까지 동일하게 맞춰 탐색으로 존재를 알아낼 수 없게 했다.

**2층**: `Commitment.room_id`를 스윕 시점에 직접 저장. 판정은 이렇게 된다.

```
source_type != 'chat'  → 그룹 공개 (통화·문서는 방 개념이 없다)
room_id IS NULL        → 그룹 공개 (방이 사라졌다 — 검사할 멤버십이 없다)
room_id 있음           → ChatRoomMember 확인
```

삭제된 방의 인용구가 그룹에 공개되는 건 받아들인 트레이드오프다. 추적 중인 약속이 조용히 사라지는 쪽이 더 나쁘다고 판단했다.

**3층**: DB 캐스케이드에 맡기지 않고 `_delete_room_cascade`가 **애플리케이션 레벨에서 직접** `room_id`를 NULL로 만든다. 두 엔진에서 같게 동작하고, 테스트가 실제 동작을 검증한다. FK와 `ON DELETE SET NULL`은 스키마에 남겼지만 이제 주 메커니즘이 아니라 경합 상황의 안전망이다.

커밋: `c2b52c2`(1층), `a2c0d45`(2·3층), `504fb62`(방어막)

### 검증

- 리뷰어가 우회 경로 **7건**을 직접 작성해 실행: `meta.total`이 숨긴 행을 세는지, `limit`이 필터보다 먼저 걸리는지, 숨긴 건과 남의 그룹 건의 403이 다른지, 일괄 요청에 섞어 부분 반영시킬 수 있는지, 방에서 쫓겨난 뒤에도 보이는지, 술어 없이 `Commitment`를 읽는 경로가 있는지, 메시지 id 재사용으로 뚫리는지. **실환경에서 통하는 것 없음.**
- 방 삭제·마지막 멤버 이탈 양쪽 경로에서 약속이 사라지지 않고 그룹에 공개되는지 회귀 테스트로 확인
- **뮤테이션 테스트**: `room_id=room.id` 한 줄을 지우고 전체 스위트를 돌렸더니 155개가 전부 통과했다. 방어막이 없다는 뜻이라 단언을 추가하고, `create_commitments`가 채팅 출처에 `room_id` 없으면 거부하도록 막았다.
- 전체 테스트 155개 통과

**검증 못 한 것**: 마이그레이션은 SQLite로 못 돌려 코드 리딩으로만 판정했다. 삭제와 스윕이 겹치는 진짜 동시성도 추론이다.

### 추후 관리

- **FK를 최후 안전망으로 남긴 건 의도적이다. 스키마에서 빼지 마라.** 삭제 트랜잭션과 스윕 INSERT가 겹칠 때 애플리케이션 처리가 동시 INSERT를 못 볼 수 있고, 그때 FK가 마무리한다.
- 이 브랜치의 이전 커밋을 이미 돌린 스테이징/프리뷰 DB가 있다면 기존 채팅 약속의 `room_id`가 NULL이라 그룹 공개로 승격된다. 해당 환경이 있으면 백필이 필요하다. 프로덕션 신규 배포에는 테이블 자체가 없어 해당 없음.
- `main.py`의 `/문서` 명령은 여전히 비공개 방 대화를 그룹 전체가 보는 `Document`로 저장한다. 같은 계열이지만 방 멤버가 명시적으로 실행하는 동작이라 이번 범위 밖으로 뒀다.

### 배운 점

**표시용으로 만든 데이터에 나중에 보안 판정을 얹으면 안 된다.** `source_id`는 "어디서 나왔는지"를 보여주려고 만든 값이고, 그래서 FK가 없어도 괜찮았다. 접근 제어가 그 위에 올라탄 순간 무결성 요구가 달라졌는데 데이터 구조는 그대로였다. 새 판정에는 그 판정을 위한 값을 따로 둬야 한다.

**개발 DB와 운영 DB가 다르면, DB가 해주는 일에 기대는 코드는 반쪽만 검증된다.** 애플리케이션이 직접 하면 느리고 장황해 보여도 두 환경에서 같게 돌고 테스트가 진짜를 검증한다.

**테스트가 통과한다는 건 방어막이 있다는 뜻이 아니다.** 보안 핵심 한 줄을 지우고 돌려보는 데 1분이면 된다. 이번엔 그걸 해서 155개가 전부 통과하는 걸 봤고, 안 했으면 "나중에 누가 리팩터링하다 인자를 떨어뜨리면 CI 초록불로 유출"이 남을 뻔했다. TS-021에서도 리뷰어에게 다이어그램을 주고 "전이를 전부 열거해서 비교하라"고 시킨 게 결정적이었다 — 검증은 시키는 방식이 결과를 만든다.

### 참고

- `commitment_service.py` — `_scan_room`, `create_commitments`
- `routers/commitments.py` — 가시성 필터 (list/PATCH/bulk-status)
- `main.py:686`·`main.py:800` — `_delete_room_cascade`를 부르는 두 경로
- 관련: TS-016·TS-018(환경 전제가 성립하지 않는 실패), TS-019(봉투 규약 구멍), TS-021(상태 기계)

---

## TS-021 · "종료 상태만 막는" 전이 검사는 상태 기계가 아니다

| | |
|---|---|
| **날짜** | 2026-08-06 |
| **영역** | BE / 도메인 로직 / 상태 기계 |
| **심각도** | High (승인 게이트가 무력화되고 감사 추적이 끊김) |
| **상태** | 해결됨 |
| **소요 시간** | 리뷰 발견 → 사용자 결정 → 수정·재검증 약 30분 |

### 증상

**배포되지 않았다.** 코드 리뷰에서 잡았다.

약속(commitment)의 상태 전이 규칙은 이렇게 설계돼 있었다.

```
proposed --확정--> confirmed --완료--> fulfilled
    |                  |
   무시               무시
    v                  v
dismissed          dismissed
```

그런데 구현된 검사는 다이어그램에 없는 전이를 두 개 통과시켰다.

1. `proposed → fulfilled` — 아무도 확정한 적 없는 약속이 완료 상태가 된다
2. `confirmed → proposed` — 확정을 되돌린다

**테스트 124개가 전부 통과하는 상태였다.** 브리프가 제공한 11개 테스트 중 이 두 케이스를 건드리는 것이 하나도 없었기 때문이다.

### 재현 조건

1. `proposed` 상태의 약속에 `PATCH /api/v1/commitments/{id}` 로 `{"status": "fulfilled"}` 전송
2. 200이 떨어지고 상태가 `fulfilled`로 바뀐다
3. 그 약속의 `confirmed_at`을 확인하면 `None`이다

### 원인

**표면**: `can_transition`이 세 가지만 검사했다 — target이 유효한 상태인가, current가 종료 상태가 아닌가, current와 target이 다른가.

**근본**: 이 세 조건은 **어떤 상태로도 갈 수 있다는 뜻**이다. 다이어그램의 화살표는 코드 어디에도 표현되지 않았다. "종료 상태를 잠근다"는 요구사항만 구현하면 상태 기계가 완성된다고 착각한 것이다.

스펙 문장도 여기에 한몫했다. 스펙은 "`fulfilled`와 `dismissed`는 종료 상태다. 종료 상태에서 다른 상태로 되돌리는 전이는 허용하지 않는다"라고만 썼다. **그림은 좁은 경로를 그리는데 문장은 넓은 규칙만 말했다.** 구현은 문장을 따랐고, 그림과 어긋났다.

**파생 피해 두 가지가 더 있다.** `apply_status`는 target이 `"confirmed"`일 때만, 그것도 `confirmed_at is None`일 때만 시각을 찍는다. 따라서

- `proposed → fulfilled`로 완료된 약속은 `confirmed_at`이 영영 `None`이다 — 언제 확정됐는지 추적할 방법이 없다
- `confirmed → proposed → confirmed`로 되돌렸다 다시 확정하면 **처음 확정 시각이 그대로 남는다** — 실제와 다른 시각이 기록된다

이 기능의 존재 이유가 "LLM이 뽑은 약속을 사람이 확인해야 추적 대상이 된다"는 승인 게이트인데, 확정을 건너뛰는 경로가 열려 있으면 게이트가 게이트가 아니다.

### 시도했지만 안 된 것

- **테스트를 더 돌려서 잡기** — 불가능했다. 이 결함은 테스트가 없어서 안 보인 것이지 테스트가 실패해서 보인 게 아니다. 124개 통과가 아무 신호도 주지 않았다.
- **`apply_status`에서 `confirmed_at`을 항상 채우는 우회** — 검토했으나 채택하지 않았다. 증상(`confirmed_at`이 빈 것)만 가리고 원인(허용되면 안 되는 전이가 통과하는 것)은 그대로 둔다.

### 해결

허용 전이를 **명시적 맵으로** 선언했다.

```python
_ALLOWED_TRANSITIONS = {
    "proposed": {"confirmed", "dismissed"},
    "confirmed": {"fulfilled", "dismissed"},
}

# can_transition:
#   target이 COMMITMENT_STATUSES에 있는지 확인한 뒤
#   target in _ALLOWED_TRANSITIONS.get(current, frozenset())
```

종료 상태는 **맵에 키 자체가 없다.** `.get(current, frozenset())`이 빈 집합을 돌려주므로 `fulfilled`·`dismissed`에서 나가는 전이 전부와 같은 상태로의 전이가 자동으로 거부된다. 별도의 `TERMINAL_STATUSES` 상수는 중복이 되어 삭제했다.

이 형태의 장점은 **허용된 것이 코드에 그대로 적혀 있다**는 점이다. 다이어그램과 한 줄씩 대조할 수 있다.

사용자 결정으로 엄격한 화살표를 택했다. `proposed → fulfilled`를 열어두는 선택지도 있었으나(이미 처리한 약속을 한 번에 완료하기 편하다), 승인 게이트가 무력화되는 대가가 더 컸다.

커밋: `7cd4d2d` (`fix: 약속 상태 전이를 화살표대로 엄격하게 제한 (확정 건너뛰기/되돌리기 차단)`)

### 검증

- 4개 상태 **16쌍 전체**를 코드 로직대로 추적해 요구 표와 일치함을 확인. 종료 상태 4행과 대각선(동일 상태) 전부 거부됨
- `grep -rn "TERMINAL_STATUSES"` — 워크트리 전체에 남은 참조 없음. 죽은 코드 아님
- 신규 테스트 4개는 응답 코드 400만 보지 않고, `db_session.expire_all()` 후 실제 row를 다시 읽어 **상태가 안 바뀐 것까지** 단언한다
- `bulk-status`에 금지 전이가 하나 섞이면 유효한 것까지 전부 롤백되는지 DB 상태로 검증
- 전체 테스트 128개 통과

### 추후 관리

- 상태를 가진 다른 도메인(문서 카테고리, 초대 수락 등)에 전이 규칙이 생기면 같은 형태의 명시적 맵을 쓴다.
- `dismissed`된 약속을 되살릴 방법은 없다. 스펙대로 "잘못 눌렀으면 새로 만든다"이다. 실사용에서 불편이 확인되면 그때 판단한다.

### 배운 점

**금지 목록으로 상태 기계를 만들면 빠뜨린 것이 전부 허용된다.** 허용 목록으로 만들면 빠뜨린 것이 전부 거부된다. 틀렸을 때 어느 쪽으로 틀리는지가 다르다.

그리고 스펙에 **그림과 문장이 같이 있으면 반드시 대조해야 한다.** 이번엔 문장이 그림보다 느슨했고, 구현은 문장만 읽었다. 리뷰어에게 다이어그램을 직접 건네고 "코드가 허용하는 전이를 전부 열거해서 이 표와 비교하라"고 시킨 것이 발견의 결정적 계기였다 — "상태 전이가 맞는지 봐달라"였으면 못 잡았을 것이다.

### 참고

- `commitment_service.py` — `can_transition`, `_ALLOWED_TRANSITIONS`, `apply_status`
- `docs/superpowers/specs/2026-08-06-autonomous-commitment-tracking-design.md` — 상태 전이 다이어그램과 승인 게이트
- 관련: TS-020 (같은 태스크에서 나온 테스트 픽스처 함정)

---

## TS-020 · 그룹 격리 테스트가 격리를 검증하지 못한다 — 픽스처가 만든 두 겹의 함정

| | |
|---|---|
| **날짜** | 2026-08-06 |
| **영역** | BE / 테스트 픽스처 / 권한 |
| **심각도** | Medium (테스트가 거짓 안심을 주거나 엉뚱하게 죽음) |
| **상태** | 해결됨 |
| **소요 시간** | 발견·확인·수정 약 20분 |

### 증상

"일괄 상태 변경 요청에 남의 그룹 약속 id가 하나라도 섞이면 전체를 거부한다"를 검증하는 테스트가 **403 단언에 도달하지도 못하고** `TypeError`로 죽었다.

```
TypeError: 'NoneType' object is not subscriptable
```

같은 파일의 다른 테스트 10개는 예상대로 404(라우트 미구현)로 실패했는데 이것만 다른 방식으로 실패했다. 에러 메시지는 응답 파싱 지점을 가리키는데 실제 원인은 그보다 두 단계 앞에 있었다.

### 재현 조건

1. 시드에서 `admin@onque.dev`가 **첫 번째로** 가입한다 → `role="admin"`, 기본 그룹 자동 생성
2. 테스트에서 두 번째 사용자 `outsider`를 가입시킨다
3. `outsider`로 `POST /api/v1/groups`를 호출해 "남의 그룹"을 만들려 한다
4. 응답의 `data`가 `None`이라 `["id"]` 접근에서 터진다

### 원인

**함정이 두 겹이었다.**

**첫 번째 — 신규 사용자는 그룹을 만들 수 없다.**

```python
# routers/auth.py:55
role="admin" if is_first_user else "member",
```

```python
# routers/groups.py:79 — POST /groups
_require_admin(current_user)
```

**확인된 사실**: `grep -n "_require_admin" routers/groups.py` → 79행에서 호출. `grep -n "admin" routers/auth.py` → 55행에 위 조건.

admin은 **DB 전체에서 가장 먼저 가입한 사용자 한 명**에게만 붙는다. 시드의 `admin@onque.dev`가 그 자리를 이미 차지했으므로 이후 가입자는 전부 `member`이고, 그룹 생성은 403 `GROUP_CREATE_FORBIDDEN`으로 막힌다. 봉투 규약에 따라 `data`는 `None`이 되고, 테스트는 그 `None`을 첨자 접근하다 죽는다.

**두 번째 — 원래 쓰려던 방법은 애초에 격리를 검증하지 못했다.**

처음 작성한 버전은 `outsider` 없이 시드가 이미 만들어 둔 두 번째 그룹(`group_b`)을 "남의 그룹"으로 썼다. 그런데 **요청을 보내는 관리자가 두 그룹 모두의 멤버**였다. `_require_group_member`는 통과하고 403은 나오지 않는다. 테스트는 초록불이 뜨지만 검증한 것은 아무것도 없다.

이쪽이 더 위험한 실패다. 두 번째 함정은 조용히 통과하기 때문이다. 첫 번째 함정(`TypeError`)이 시끄럽게 터져준 덕분에 두 번째를 들여다보게 됐다.

### 시도했지만 안 된 것

- **시드된 `group_b`를 그대로 쓰기** — 위 두 번째 이유로 403이 안 나온다. 통과하지만 무의미하다.
- **`outsider`를 admin으로 승격시킨 뒤 API로 그룹 생성** — 동작은 하지만 채택하지 않았다. 이 테스트가 검증하려는 것과 무관한 권한 상태를 만들고, 나중에 읽는 사람이 "여기 왜 admin이 필요하지?"에서 멈춘다.

### 해결

`db_session`으로 `Group`을 **직접 시딩**했다. 같은 테스트 파일이 이미 `Client`/`Commitment`를 그렇게 시딩하고 있어 스타일도 일치한다.

핵심은 **`GroupMembership`을 만들지 않는 것**이다. 요청을 보내는 관리자가 그 그룹의 멤버가 아니어야 `_require_group_member`가 403을 던진다.

이 테스트가 검증하려는 건 "남의 그룹 id가 섞이면 전체 거부"이지 **그 그룹이 어떻게 만들어졌는지가 아니다.** API를 거치는 건 우연한 수단이었고, 그 수단이 무관한 권한 규칙에 걸린 상황에서는 직접 시딩이 의도를 더 정확히 표현한다.

커밋: `fd706c0` (`feat: 약속 조회/상태 변경 API와 기한 계산`)

### 검증

- 교체한 테스트가 실제로 403을 받는지 확인
- **역방향 확인**: 격리 검사(`_require_group_member` 루프)를 제거하면 이 테스트가 200 `updated: 2`로 바뀌어 실패한다. 즉 검증 대상을 실제로 붙잡고 있다
- 전체 테스트 124개 통과

### 추후 관리

- 이 저장소에서 "다른 그룹" 격리를 테스트할 때는 **요청자가 그 그룹의 멤버가 아닌지**를 먼저 확인해야 한다. 시드된 관리자는 시드 그룹 전부의 멤버다.
- 신규 사용자로 그룹을 만들어야 하는 테스트는 앞으로도 같은 벽에 부딪힌다. 직접 시딩이 기본 수단이다.
- 권한 규칙 자체("첫 가입자만 admin")는 실제 운영에서도 제약이다. 두 번째 팀이 독립적으로 가입해 자기 그룹을 만들 수 없다. 이번 범위 밖이라 손대지 않았으나 별도 판단이 필요하다.

### 배운 점

**격리 테스트는 통과했다는 사실만으로 아무것도 증명하지 않는다.** 격리 로직을 지웠을 때 그 테스트가 실패하는지 확인해야 비로소 검증이 된다. 이번 건은 그 역방향 확인을 했기 때문에 두 번째 함정을 잡았다.

그리고 픽스처가 만든 상태(누가 admin인지, 누가 어느 그룹의 멤버인지)는 테스트 본문에 안 보인다. 안 보이는 전제가 테스트의 의미를 통째로 무효화할 수 있다.

### 참고

- `routers/auth.py:55` — 첫 가입자만 admin
- `routers/groups.py:79` — 그룹 생성은 admin만
- `tests/test_commitment_routes.py` — `test_bulk_status_rejects_all_if_one_is_foreign`

---

## TS-019 · 사전 `SELECT` 중복 검사는 경합에서 API 봉투 규약을 깬다

| | |
|---|---|
| **날짜** | 2026-08-06 |
| **영역** | BE / API 계약 |
| **심각도** | Medium (간헐적, 우회 가능하나 프론트 분기가 깨짐) |
| **상태** | 예방됨 — 코드 리뷰에서 걸러 배포되지 않음 |
| **소요 시간** | 리뷰 지적 후 수정·검증 약 5분 |

### 증상

**발생하지 않았다.** 자율 약속 추적 기능(Task 2, 클라이언트 등록 API)을 구현하고 리뷰하는 과정에서 배포 전에 잡았다. TS-018과 같은 "회피한 사고"의 기록이다.

배포됐다면 나타났을 증상: 같은 클라이언트 이름으로 두 사람이 **동시에** 등록 버튼을 누를 때, 한쪽은 정상적인 409 대신 봉투가 없는 순수 500을 받는다.

정상 경로의 응답:

```json
{"success": false, "data": null,
 "error": {"code": "CLIENT_NAME_DUPLICATE", "message": "이미 등록된 클라이언트입니다"}}
```

경합 시의 응답:

```
Internal Server Error
```

프론트엔드는 `error.message`가 아니라 `error.code`로 분기하도록 설계돼 있다(`api-contract.md`). 봉투가 없으면 `error`를 읽는 순간 터지거나 "알 수 없는 오류"로 떨어진다.

### 재현 조건

1. 유니크 제약이 걸린 테이블에 "사전 `SELECT`로 중복 확인 → 없으면 `INSERT`" 방식의 생성 엔드포인트를 만든다
2. 같은 값으로 두 요청을 동시에 보낸다
3. 둘 다 `SELECT`를 통과한 뒤 한쪽이 DB 제약에서 튕긴다

간헐적으로만 재현된다. 순차 요청으로는 절대 나오지 않으므로 일반적인 수동 테스트와 단위 테스트로는 잡히지 않는다.

### 원인

**표면**: 중복 검사와 삽입 사이에 시간 간격이 있다(TOCTOU).

**근본**: 이 프로젝트의 봉투 규약이 **`HTTPException`을 통해서만** 적용된다는 점이다.

`main.py:42`에는 핸들러가 하나뿐이다.

```python
@app.exception_handler(FastAPIHTTPException)
async def http_exception_handler(request, exc: FastAPIHTTPException):
    if isinstance(exc.detail, dict):
        error = exc.detail
    else:
        error = {"code": "INTERNAL_ERROR", "message": str(exc.detail)}
    return JSONResponse(
        status_code=exc.status_code,
        content={"success": False, "data": None, "error": error},
    )
```

**확인된 사실**: `grep -n "exception_handler" main.py` 결과 핸들러는 이 한 개뿐이다. 범용 `Exception` 핸들러가 없다.

따라서 `HTTPException`이 아닌 예외 — `sqlalchemy.exc.IntegrityError`를 포함해 — 는 이 핸들러를 타지 않고 Starlette 기본 500(평문 `Internal Server Error`)으로 나간다. 코드에 `raise` 한 줄 없이도 봉투 규약이 조용히 깨지는 경로가 열려 있는 셈이다.

즉 이건 "동시성 버그"라기보다 **예외 종류에 따라 응답 형식이 달라지는 구조적 구멍**이다. 유니크 제약이 있는 모든 신규 엔드포인트에서 같은 방식으로 재발한다.

### 시도했지만 안 된 것

- **사전 `SELECT` 검사를 강화하는 방향**은 원리적으로 불가능하다. 검사와 삽입이 별개 문장인 이상 간격은 없어지지 않는다. 트랜잭션 격리 수준을 올려도 결국 한쪽은 실패하며, 실패 형태가 `IntegrityError`인 것은 그대로다.
- **사전 검사를 제거하고 DB 제약에만 맡기는 방향**도 답이 아니다. 그러면 정상적인 순차 중복 요청까지 전부 500이 된다. 사전 검사는 정상 경로에서 더 명확한 에러를 주는 역할이 있으므로 남겨야 한다.

### 해결

사전 검사는 유지하고, `commit()`을 `IntegrityError`로 감싸 **같은 409로 수렴**시킨다.

```python
existing = db.execute(
    select(Client).where(Client.group_id == body.group_id, Client.name == name)
).scalar_one_or_none()
if existing:
    raise HTTPException(409, detail={"code": "CLIENT_NAME_DUPLICATE", ...})

db.add(client)
try:
    db.commit()
except IntegrityError:
    db.rollback()
    raise HTTPException(409, detail={"code": "CLIENT_NAME_DUPLICATE", ...})
```

`db.rollback()`이 `raise`보다 **먼저** 와야 한다. 롤백 없이 빠져나가면 세션이 오염된 채 요청의 나머지 구간으로 흘러간다.

커밋: `33f9da8` (`fix: 클라이언트 API 개선 (경합 조건 해결, 페이지네이션 추가)`)

### 검증

- 재리뷰에서 `IntegrityError`의 import 위치(`sqlalchemy.exc`), `rollback()` 호출 순서, 그리고 두 409 경로의 응답 형태가 `status_code`·`detail` 모두 동일함을 확인
- 전체 테스트 102개 통과

**검증하지 못한 것**: 실제 동시 요청을 발생시키는 경합 테스트는 작성하지 않았다. 정상 경로와 순차 중복 경로만 테스트로 덮여 있고, `except IntegrityError` 분기는 코드 검토로만 확인했다.

### 추후 관리

- 유니크 제약이 있는 테이블에 생성 엔드포인트를 추가할 때마다 같은 처리를 반복해야 한다. 반복이 3번을 넘으면 헬퍼로 뽑는다.
- **범용 `Exception` 핸들러를 추가하는 편이 근본 대책이다.** 어떤 예외가 새더라도 `traceId`를 담은 `INTERNAL_ERROR` 봉투로 나가게 하면 이 부류의 구멍이 한 번에 막힌다. 이번 범위 밖이라 하지 않았다.
- `api-contract.md`는 500 응답에 스택 트레이스를 담지 말 것을 요구한다. 범용 핸들러를 넣을 때 함께 지켜야 한다.

#### 후속 (2026-08-09)

**재발 지점이 하나 더 있었고, 위에 적어둔 근본 대책을 실제로 넣었다.** → TS-027

- **재발**: `POST /groups/{id}/invitations`가 정확히 같은 형태였다 — 사전 `SELECT`로 `GROUP_INVITE_ALREADY_SENT`를 검사한 뒤 `INSERT`. 관리자 두 명이 같은 이메일을 동시에 초대하거나 한 명이 더블클릭하면 유니크 제약(`uq_group_invitations_group_email`)에서 진 쪽이 봉투 없는 500으로 터진다. 이번엔 `IntegrityError`를 잡아 **순차 실행이었다면 받았을 409 `GROUP_INVITE_ALREADY_SENT`** 로 답하게 했다. 새 에러 코드를 만들지 않고 기존 것을 재사용했다 — 경합 여부는 클라이언트가 알 필요가 없다.
- **근본 대책 반영**: 범용 `Exception` 핸들러를 넣었다. 스택 트레이스와 내부 경로는 서버 로그에만 남기고 응답엔 `INTERNAL_ERROR`만 내려간다. `traceId`는 아직 없다 — 요청 단위 컨텍스트 전파(`contextvars`)가 필요해 별건으로 남긴다.
- 그 과정에서 **핸들러가 애초에 404·405를 못 잡고 있었다**는 더 큰 구멍이 드러났다(TS-027). 이 항목이 "핸들러를 우회한다"고 쓴 것보다 실제 상황이 나빴던 셈이다.

### 배운 점

에러 응답 형식을 예외 타입에 **의존**시키면, 그 타입을 벗어나는 순간 규약이 조용히 깨진다. 봉투를 지키는 코드가 어디에도 없는 게 아니라 — 있는데 안 타는 것이다. 그래서 코드만 읽으면 정상으로 보인다.

TS-016·TS-018과 같은 계열이다. 셋 다 "코드는 설계대로 동작하는데 전제가 성립하지 않는" 실패다. 예외도 로그도 남지 않는다는 점까지 같다.

### 참고

- `routers/commitments.py` — `create_client`
- `main.py:42` — 유일한 예외 핸들러
- `.claude/standards/api-contract.md` — 봉투 형식과 에러 코드 규약

---

## TS-018 · 접근 규칙을 좁히는 스키마 변경을 백필 없이 배포하면 전원이 잠긴다

| | |
|---|---|
| **날짜** | 2026-08-05 |
| **영역** | Infra / DB / 배포 |
| **심각도** | High (발생했다면 핵심 기능 불가) |
| **상태** | 예방됨 — 배포 전에 백필을 실행해 실제 장애는 발생하지 않음 |
| **소요 시간** | 백필 스크립트 작성·실행 약 10분 |

### 증상

**실제로는 발생하지 않았다.** 배포 전에 막았기 때문이다. 이 항목은 사후 분석이 아니라 회피한 사고의 기록이다.

배포했다면 나타났을 증상: 로그인 후 팀 채팅에 들어가면 **모든 사용자에게 방 목록이 비어 있고**, 기존 방의 대화 이력에 접근하면 403이 떨어진다. 데이터는 하나도 지워지지 않았는데 아무도 볼 수 없는 상태다.

```json
{"success": false, "data": null,
 "error": {"code": "CHAT_ROOM_FORBIDDEN", "message": "이 채팅방에 초대되지 않았습니다."}}
```

### 재현 조건

1. 방 접근 규칙을 "그룹원이면 누구나"에서 "그룹원 **이면서** 방에 초대된 사람"으로 변경
2. 초대 명단을 담는 `chat_room_members` 테이블을 새로 추가
3. **백필 없이** 배포 — `Base.metadata.create_all(bind=engine)`이 빈 테이블을 만든다
4. 로그인 → 팀 채팅

### 원인

**근본**: `create_all()`은 **테이블을 만들 뿐, 채워주지 않는다.**

TS-016에서 이미 "`create_all()`이 기존 테이블을 ALTER하지 않는다"를 기록했다. 이번 건은 그것과 **다른 실패 모드**다. 이번엔 테이블 생성 자체는 정상으로 동작한다. 문제는 새 테이블이 **기존 데이터에 대한 판단을 담고 있다**는 점이다.

새 접근 검사는 이렇게 생겼다.

```python
if not db.get(ChatRoomMember, {"room_id": room.id, "user_id": user.id}):
    raise HTTPException(status_code=403, detail={"code": "CHAT_ROOM_FORBIDDEN", ...})
```

빈 테이블에서 이 조회는 **항상 None**이다. 즉 "초대 기록이 없음"이 곧 "접근 거부"로 해석된다. 코드는 정확히 설계대로 동작하고, 예외도 로그도 남지 않는다. 그래서 더 위험하다 — TS-016은 컬럼이 없어 500이 터졌지만, 이번엔 **정상 응답 형태의 403**이라 "권한 설정이 원래 그런가 보다"로 오해되기 쉽다.

**확인된 사실**: 운영 DB의 방은 2개(그룹당 기본방), 그룹원은 각 1명이었다. 백필 대상은 총 2행으로 규모가 작았지만, 행 수와 무관하게 0행이면 전원이 잠긴다.

### 시도했지만 안 된 것

**이번엔 실패한 시도가 없다.** 정직하게 적어둔다 — 이건 내가 잘해서가 아니라 **TS-016을 이미 겪었기 때문**이다. 스키마를 건드릴 때 "운영 DB의 기존 행은 어떻게 되나"를 먼저 묻는 습관이 그 기록에서 나왔다.

한 가지 예측된 실패는 있었다. 로컬 테스트 `test_delete_room_forbidden_for_other_member`가 깨졌다.

```
E       AssertionError: assert 'CHAT_ROOM_FORBIDDEN' == 'CHAT_ROOM_DELETE_FORBIDDEN'
E         - CHAT_ROOM_DELETE_FORBIDDEN
E         + CHAT_ROOM_FORBIDDEN
```

이건 버그가 아니라 **테스트가 검증 대상을 잃은 것**이다. 그룹원이지만 방에 초대되지 않은 사람은 이제 삭제 권한 검사에 도달하기도 전에 입장에서 막힌다. 테스트에 초대 단계를 넣어 원래 검증하려던 것(방 멤버지만 방장은 아닌 사람의 삭제 시도)을 되살렸다.

### 해결

배포 **전에** 백필을 실행했다. 순서가 핵심이다.

```python
# scripts/migrate_add_chat_room_members.py
Base.metadata.create_all(bind=engine)   # 1) 테이블 생성

with engine.begin() as conn:            # 2) 그룹원 × 그 그룹의 방
    conn.execute(text("""
        INSERT INTO chat_room_members (room_id, user_id)
        SELECT r.id, m.user_id
        FROM chat_rooms r
        JOIN group_memberships m ON m.group_id = r.group_id
        ON CONFLICT (room_id, user_id) DO NOTHING
    """))
```

`ON CONFLICT DO NOTHING` 덕에 여러 번 돌려도 안전하다. 기존 테이블은 건드리지 않는 **추가 전용**이라, 되돌리려면 `DROP TABLE chat_room_members` 하나면 된다.

### 검증

스크립트가 자체 검사까지 하도록 만들었다. 사람이 눈으로 확인하는 대신 실패하면 죽게 했다.

```
chat_room_members: 0 → 2 (신규 2행)
멤버 없는 방: 0개
```

```python
assert orphan == 0, "멤버가 없는 방이 남았다. 그룹원이 0명인 그룹이 있는지 확인할 것."
```

`orphan`은 "멤버가 한 명도 없는 방"의 개수다. 이 값이 0이 아니면 그 방은 아무도 못 여는 상태이므로, 그대로 배포하면 안 된다.

로컬 테스트도 79건 통과(신규 11건). 그중 하나는 초대받지 않은 그룹원이 방 목록에서 그 방을 못 보는지를 고정한다.

### 대조군 — 같은 날 두 번째 마이그레이션은 백필이 필요 없었다

이어서 `group_invitations` 테이블을 추가했는데, 이건 백필 없이 `create_all`만으로 충분했다. 차이가 이 항목의 핵심이다.

| | `chat_room_members` | `group_invitations` |
|---|---|---|
| 기존 행에 대한 올바른 기본값 | **없음** — 빈 값이 "거부"를 뜻함 | 있음 — 빈 값이 "초대 없음"으로 맞음 |
| 백필 | **필수** | 불필요 |

판별 질문은 하나다. **"이 테이블이 비어 있을 때 기존 사용자에게 벌어지는 일이 옳은가?"** 옳으면 백필이 필요 없고, 옳지 않으면 배포 전에 채워야 한다.

### 추후 관리

- **배포 순서 규칙**: 접근·권한을 좁히는 변경은 `백필 → 코드 배포` 순서를 지킨다. 반대로 하면 그 사이 시간만큼 전원이 잠긴다.
- Alembic 도입은 여전히 미해결 과제다(TS-016 추후 관리에도 동일하게 적어둠). 지금은 스키마 변경마다 `scripts/migrate_*.py`를 수기로 만들고 있고, 실행 여부가 어디에도 기록되지 않는다. 같은 스크립트를 두 번 돌렸는지 알 방법이 없어 전부 멱등하게 작성하는 것으로 버티고 있다.
- 백필 스크립트에는 **성공 조건을 `assert`로 넣는다.** 출력만 찍고 넘어가면 사람이 안 읽는다.

### 배운 점

**스키마 마이그레이션에는 두 종류가 있다.** 구조만 바꾸는 것과, **기존 데이터에 새로운 의미를 부여해야 하는 것**. `create_all()`은 앞의 것만 한다. 뒤의 것을 잊으면 코드는 완벽하게 동작하면서 사용자를 전부 잠근다.

특히 위험한 건 **이 실패가 조용하다**는 점이다. 500도 스택 트레이스도 없고, 설계된 대로의 403이 나온다. 로그를 뒤져도 이상한 게 안 보인다. 그래서 "빈 테이블이 뜻하는 바"를 배포 전에 스스로 물어보는 것 말고는 이걸 잡을 방법이 없다.

---

## TS-017 · 새 엔드포인트 404 — 배포 지연이었으나 잘못된 근거로 두 번 오판

| | |
|---|---|
| **날짜** | 2026-08-05 |
| **영역** | Infra / 배포 |
| **심각도** | Medium |
| **상태** | 해결됨 (반영 완료) |
| **소요 시간** | 약 30분 |

### 증상

채팅방 기능을 배포한 뒤 사용자가 "방 만들기가 안 된다"고 보고. 프론트엔드는 `POST /chat/rooms`를 호출하는데 백엔드에 그 경로가 없었다. 사용자는 Render에서 **배포가 성공 상태**라고 확인해줬다.

### 재현 조건

1. 백엔드 변경을 push
2. Render 대시보드에는 배포 성공으로 표시
3. 그러나 실제 서비스는 이전 커밋 코드를 계속 서빙

### 원인

**근본**: Render 배포 반영이 20분 이상 지연됐다. 결국 별도 조치 없이 반영됐다.

**확인된 사실**: 실행 중인 백엔드가 정확히 어느 커밋인지는 **엔드포인트 파라미터 이름으로 특정**할 수 있었다.

```
/summarize-document 파라미터: [group_id, auto_todo, ...]   -> 앞 커밋은 반영됨
/chat/messages GET 파라미터:  [group_id, ...]              -> 최신 커밋 미반영 (room_id여야 함)
/chat/rooms:                  없음
```

이 조합으로 "배포가 아예 안 됐다"가 아니라 "정확히 한 커밋 뒤에 있다"를 알 수 있었다.

### 시도했지만 안 된 것 — 두 번의 오판

**오판 1: 백엔드 URL을 추측했다.** `onque-backend.onrender.com`으로 찔러보고 404가 나오자 서비스 상태를 논하기 시작했다. 그런데 그 응답은 FastAPI도 Render의 404도 아닌 **Express의 `Cannot GET /`** 였다 — 완전히 남의 서비스였다. 응답 본문을 봤으면 바로 알았을 것을, 상태 코드만 보고 넘어갔다.

실제 주소는 `onque-backend-la7e.onrender.com`이었고, `NEXT_PUBLIC_` 변수는 번들에 인라인되므로 배포된 프론트엔드 청크에서 찾을 수 있었다.

```bash
grep -oE 'src="/_next/static/immutable/chunks/[^"]+"' page.html | ... \
  | xargs -I{} curl -s "https://<앱>{}" \
  | grep -oE 'https://[a-zA-Z0-9._-]+\.onrender\.com'
```

**오판 2: CDN 캐시본을 보고 "프론트도 구버전"이라 결론냈다.** 배포된 CSS에서 옛 색상 토큰(`#3452eb`, `#f5f6f8`)이 나오길래 프론트엔드도 반영이 안 됐다고 사용자에게 보고했다. 실제로는 **캐시된 옛 HTML에 링크된 옛 CSS 파일**을 받아본 것이었다(`x-vercel-cache: HIT`, `age: 286`). 프론트엔드는 최신이 맞았고, 번들 JS에 `chat/rooms`·`room_id`·`새 채팅방 이름`이 모두 들어 있었다.

이 오판은 사용자에게 잘못된 상황 설명이 나간 뒤 정정해야 했다.

**추가로 세운 빗나간 가설**: URL의 `-la7e` 접미사가 이름 충돌 흔적이라는 점에 근거해 "Render 서비스가 두 개이고 배포된 쪽과 프론트가 보는 쪽이 다르다"고 추정했다. 정황은 그럴듯했지만 사실이 아니었다. 단순 반영 지연이었다.

### 해결

기다리자 반영됐다. 근본 조치는 없었다.

```
/chat/rooms 존재: True
/chat/messages GET 파라미터: [room_id, authorization]
```

### 추후 관리

- Render 무료 티어는 push → 반영까지 수십 분이 걸릴 수 있다. 배포 직후 확인이 실패해도 곧바로 "실패"로 단정하지 말 것.
- 강제 수단: Render 대시보드 → **Manual Deploy → Clear build cache & deploy**.
- 배포 버전 확인이 자주 필요하다면 `/health` 응답에 커밋 SHA를 넣는 게 가장 확실하다. 지금은 매번 엔드포인트 시그니처로 역추적하고 있다.

### 배운 점

**배포 버전은 "겉모습"이 아니라 API 계약으로 특정해야 한다.** 색상 토큰이나 화면 문구는 CDN 캐시·프리렌더 때문에 얼마든지 거짓말을 한다. 반면 `openapi.json`의 파라미터 이름은 실행 중인 코드에서 직접 나오므로 속지 않는다. 실제로 `group_id`냐 `room_id`냐 하나로 커밋 시점이 특정됐다.

그리고 **주소를 추측해서 찌르지 말 것.** 추측한 URL에서 받은 404를 근거로 서비스 상태를 논하는 순간 전체 추론이 무너진다. 응답 본문을 한 줄이라도 봤다면 Express 응답임을 즉시 알았을 것이다. 상태 코드만 보고 판단한 것이 문제였다.

---

## TS-016 · 프로덕션 DB가 구스키마에 머물러 4개 기능이 죽어 있었으나 화면상 정상으로 보임

| | |
|---|---|
| **날짜** | 2026-08-05 |
| **영역** | Infra / DB / UX |
| **심각도** | High |
| **상태** | 해결됨 |
| **소요 시간** | 약 15분 (발견 후 조치) |

### 증상

`Document.summary_json` 컬럼을 추가하고 배포한 뒤, Neon에 컬럼이 실제로 생겼는지 확인하려고 스키마를 조회했다. 그 과정에서 **의도치 않게 훨씬 큰 문제가 드러났다.**

```
documents 컬럼: ['id', 'source_type', 'category', 'filename', 'summary', 'created_at']
```

`group_id`도 `is_template`도 없었다. 그룹 기능 도입 전 스키마 그대로였다. 전체 테이블을 조회하니 4개가 같은 상태였다.

```
chat_messages (행 0): ['id', 'sender', 'content', 'is_bot', 'created_at']
documents     (행 0): ['id', 'source_type', 'category', 'filename', 'summary', 'created_at']
schedules     (행 0): ['id', 'title', 'scheduled_date', 'created_at', 'updated_at']
todos         (행 0): ['id', 'content', 'due_date', 'is_done', 'created_at', 'updated_at']

announcements     (행 0): 정상
groups            (행 1): 정상
group_memberships (행 1): 정상
users             (행 1): 정상
```

즉 배포된 앱에서 **할 일·일정·채팅·문서 관련 요청이 이미 전부 실패하고 있었다.** 이 코드들은 모두 `WHERE group_id = ...`로 조회한다.

### 재현 조건

1. 그룹 기능 도입 이전에 생성된 Neon 인스턴스를 그대로 사용
2. 그룹 기능이 포함된 백엔드를 배포 (`Base.metadata.create_all(bind=engine)` 실행됨)
3. 로그인 → 대시보드 진입

### 원인

**표면**: 브라우저로 확인했을 때 로그인·그룹 생성·공지 등록이 모두 정상 동작해 배포가 성공한 것으로 판단했다.

**근본 (두 겹)**

첫째, **`Base.metadata.create_all()`은 없는 테이블만 만들고, 이미 존재하는 테이블에는 컬럼을 추가하지 않는다.** 이름이 "create_all"이라 전체를 최신 상태로 맞춰줄 것처럼 읽히지만 실제 동작은 `CREATE TABLE IF NOT EXISTS`에 가깝다. 마이그레이션 도구(Alembic 등) 없이 이것만 쓰면, 모델에 컬럼을 추가해도 기존 테이블은 영원히 옛 모양으로 남는다.

둘째, **프론트엔드가 그 실패를 조용히 삼켰다.** `WorkspaceContext.refresh()`의 `catch`는 주석까지 달아 의도적으로 침묵하고 있었고, 대시보드의 문서 조회도 `.catch(() => setDocuments([]))`였다.

```ts
} catch {
  // 대시보드 패널은 조용히 실패한다 — 원인 파악은 채팅/업로드 화면의 에러 메시지에서 이뤄진다.
}
```

그 결과 화면에서 **"항목이 0개"와 "요청이 실패함"이 완전히 동일하게 보였다.** 신규 배포라 데이터가 없는 게 자연스러운 상황이었기 때문에, 빈 목록을 정상으로 읽고 넘어갔다.

### 시도했지만 안 된 것

- **브라우저 E2E 확인**: 로그인 → 대시보드 → 그룹 생성 → 공지 등록을 실제로 수행하고 "검증 완료"로 판단했다. 하필 정상 동작한 네 가지(`users`, `groups`, `group_memberships`, `announcements`)가 모두 스키마가 맞는 테이블이었고, 깨진 네 가지는 전부 침묵하는 경로였다. **UI 관찰만으로는 이 장애를 잡을 수 없었다.**
- 컬럼 추가 후 "`create_all`이 처리하겠지"라고 가정한 것 — 실제로는 `ALTER TABLE`을 하지 않는다.

### 해결

대상 4개 테이블이 모두 **행 0개**임을 먼저 확인한 뒤(참조하는 외래키도 없음) 삭제하고 현재 모델로 재생성했다. 행이 0이 아니면 중단하도록 assert를 걸고 실행했다.

```python
with engine.begin() as c:
    for t in ['chat_messages', 'documents', 'schedules', 'todos']:
        n = c.execute(text(f'SELECT count(*) FROM {t}')).scalar()
        assert n == 0, f'{t}에 {n}행이 있어 중단'
        c.execute(text(f'DROP TABLE {t}'))

Base.metadata.create_all(bind=engine)
```

`users`·`groups`·`group_memberships`는 실제 계정 데이터가 있어 건드리지 않았다.

동시에 **침묵하던 예외 처리를 걷어냈다.**
- `WorkspaceContext`에 `error: string | null`을 추가하고 `refresh()`의 `catch`가 이를 설정하도록 변경
- `SmartDashboardPanel`과 대시보드에 경고 배너 추가 — "비어 있는 것이 아니라 조회에 실패한 상태"라고 명시

### 검증

```
chat_messages ['id', 'group_id', 'sender', 'content', 'is_bot', 'created_at']
documents     ['id', 'group_id', 'is_template', 'source_type', 'category', 'filename',
               'summary', 'summary_json', 'created_at']
schedules     ['id', 'group_id', 'title', 'scheduled_date', 'created_at', 'updated_at']
todos         ['id', 'group_id', 'content', 'due_date', 'is_done', 'created_at', 'updated_at']
```

`pytest` 50건 / `tsc --noEmit` / `next build` 모두 통과.

### 추후 관리

- **`create_all`만으로는 스키마 변경을 배포할 수 없다.** 모델에 컬럼을 추가할 때마다 수동 `ALTER TABLE`이 필요하며, 지금처럼 놓치기 쉽다. 데이터가 쌓이기 시작하면 Alembic 도입이 사실상 필수다. (현재는 데이터가 거의 없어 drop & recreate로 넘어감)

**재발 1 (2026-08-05, 같은 날)** — 채팅방(`ChatRoom`) 기능을 넣으며 `chat_messages.group_id`를 `room_id`로 바꿨다. 신규 테이블 `chat_rooms`는 `create_all`이 만들어줬지만 `chat_messages`는 예상대로 옛 컬럼을 유지했다. 이번엔 알고 있었으므로 배포 전에 확인하고 처리했다.
```python
n = c.execute(text('SELECT count(*) FROM chat_messages')).scalar()
assert n == 0, f'chat_messages에 {n}행이 있어 중단'
c.execute(text('DROP TABLE chat_messages'))
Base.metadata.create_all(bind=engine)
```
더불어 기존 그룹은 방 없이 남으므로, 그룹마다 기본 방("일반")을 채워 넣는 보정이 필요했다. **모델 변경이 기존 행에 대한 백필까지 요구하는 경우가 있다는 점**이 이번에 추가로 드러났다 — 스키마만 맞춘다고 끝이 아니다.
- 배포 후 확인 항목에 **DB 스키마 대조**를 넣어야 한다. UI 관찰로는 침묵하는 실패를 못 잡는다.
- 남아 있는 조용한 `catch`가 더 있는지 주기적으로 점검할 것.

### 배운 점

**빈 화면은 "데이터가 없다"는 뜻이 아니라 "아무것도 모른다"는 뜻이다.** 신규 배포 직후처럼 빈 상태가 자연스러운 상황에서는 이 둘을 구분할 단서가 화면에 전혀 없다. 오류를 삼키는 `catch`는 그 순간에는 화면을 깔끔하게 만들어주지만, 정확히 장애를 발견해야 할 때 발견을 막는다.

그리고 **"E2E로 확인했다"는 확인한 경로에 대해서만 참이다.** 이번엔 정상 동작한 기능들이 우연히 전부 스키마가 맞는 쪽이었고, 깨진 쪽은 전부 침묵했다. 확인했다고 말하기 전에 "무엇을 확인했고 무엇을 확인하지 않았는가"를 나눠서 봐야 한다.

---

## TS-015 · 요약 리포트의 이모지가 프론트엔드 수정으로 사라지지 않음 (출처가 LLM 프롬프트)

| | |
|---|---|
| **날짜** | 2026-08-05 |
| **영역** | BE / UX |
| **심각도** | Medium |
| **상태** | 해결됨 |
| **소요 시간** | 약 20분 (원인 파악), 이후 구조 전환 |

### 증상

"UI에 애플 이모티콘 쓰는 것을 자제해달라"는 요구에 따라 프론트엔드 전반의 이모지를 제거했는데도, 통화 요약과 문서 요약 결과 화면에는 이모지가 그대로 남았다.

```
[📌 한 줄 요약]
- 9월 출시 일정을 확정함

[💬 주요 논의 내용]
- ...

[🙋‍♀️ 고객/상대방 요구사항]
- ...

[✅ 다음 액션 아이템]
- ...

[📝 기타 메모]
- ...
```

### 재현 조건

1. `/calls` 또는 `/documents`에서 파일 업로드 → 요약 실행
2. 결과 리포트 또는 `/history`에서 해당 항목 펼치기
3. 프론트엔드 소스 전체를 이모지로 grep해도 아무것도 나오지 않음

### 원인

**표면**: 프론트엔드 어딘가에 이모지가 남아 있다고 판단했다. 실제로는 프론트엔드 코드에 해당 이모지가 한 글자도 없었다.

**근본**: `gemini_service.py`의 `CALL_SUMMARY_PROMPT` / `DOCUMENT_SUMMARY_PROMPT`가 LLM에게 이모지 섹션 헤더를 **출력 형식으로 강제**하고 있었다.

```python
CALL_SUMMARY_PROMPT = """
...
[📌 한 줄 요약]
- 통화 내용을 한두 문장으로 요약

[💬 주요 논의 내용]
...
규칙:
- 출력은 반드시 위와 같은 섹션 제목 형식을 그대로 사용할 것.
"""
```

그 응답 문자열이 통째로 `documents.summary` TEXT 컬럼에 저장되고(`main.py`), 화면에서는 `<pre>{summary}</pre>`로 그대로 출력됐다(`UploadPanel.tsx`, `history/page.tsx`). 즉 **화면에 보이는 이모지의 출처는 코드가 아니라 프롬프트**였고, 렌더링 계층에는 손댈 지점이 아예 없었다.

같은 원인으로 세 가지가 동시에 막혀 있었다는 점도 이때 드러났다:
- 요약을 섹션 단위로 다룰 수 없음 (한 줄 요약만 카드에 띄우기, 액션 아이템만 할 일로 넘기기 불가)
- 대시보드에 집계할 구조가 없어 정적 카드 나열이 한계
- 이모지 제거 불가

### 시도했지만 안 된 것

- **프론트엔드 전역 이모지 grep 후 제거**: `Sidebar.tsx`, `MobileNav.tsx`, `dashboard/page.tsx` 등에서 8건을 찾아 제거했다. 실제로 남아 있던 것들이라 제거 자체는 유효했지만, **요약 리포트의 이모지는 그대로였다.**
- **프론트엔드에서 정규식으로 이모지 치환하는 방안 검토**: LLM 출력 형식이 흔들리면 같이 깨지고, 근본 원인을 화면단에서 덮는 방식이라 채택하지 않았다.

### 해결

프롬프트를 이모지 섹션 헤더 대신 **`response_schema` 기반 구조화 JSON**으로 교체했다. 같은 파일의 `classify_document_category`와 `extract_chat_actions`가 이미 쓰던 방식이라 새 의존성은 없었다.

```python
_SUMMARY_SCHEMA = {
    "type": "OBJECT",
    "required": ["headline", "key_points", "requests", "action_items", "notes"],
    "properties": {
        "headline": {"type": "STRING"},
        "key_points": {"type": "ARRAY", "items": {"type": "STRING"}},
        "requests": {"type": "ARRAY", "items": {"type": "STRING"}},
        "action_items": {...},
        "notes": {"type": "STRING"},
    },
}
```

부수 조치:
- `Document.summary_json` (Text, nullable) 추가. 평문 사본은 `summary`에 계속 저장해 이력 검색(문자열 매칭)과 구버전 레코드 호환을 유지.
- `normalize_summary()`로 저장 전 필드 보정 (빈 content 제거, 알 수 없는 priority → `normal`, 리스트 아닌 값 → `[]`).
- 오디오 + `response_schema` 조합이 실패할 여지가 있어, 파싱 실패 시 스키마 없이 재호출해 평문만 확보하는 폴백을 넣었다. 이 경우 `summary_json`은 `NULL`이 되고 프론트는 평문으로 렌더한다.

### 검증

- `pytest` 50건 통과 (신규 9건: 정규화 보정, 평문 렌더, 구조화 저장, `auto_todo` 할 일 생성, 폴백 경로, `POST /todos` 권한 3건)
- 실제 Gemini 호출로 합성 회의록을 요약 — 스키마 준수 확인. 상대 날짜("8월 12일까지")가 `"2026-08-12"` 절대 날짜로, 급한 건이 `priority: "high"`로 정확히 매핑됐고 **출력 전체에 이모지 없음**
- `npx tsc --noEmit` / `next build` 통과

### 추후 관리

- **기존 Postgres(Neon) 테이블에는 `create_all`이 컬럼을 추가하지 않는다.** DB를 리셋하지 않는 경우 아래가 필요하다.
  ```sql
  ALTER TABLE documents ADD COLUMN summary_json TEXT;
  ```
- 구버전 레코드는 `structured: null`로 내려가 평문 폴백으로 렌더된다. 데이터 마이그레이션은 하지 않았다.
- 폴백 경로가 조용히 동작하므로, `summary_json IS NULL` 비율이 올라가면 스키마 준수가 깨지고 있다는 신호다.

### 배운 점

LLM 출력이 화면에 그대로 나오는 구간에서는, **보이는 문자열의 출처가 코드가 아니라 프롬프트일 수 있다.** 프론트엔드 grep이 아무것도 찾지 못했을 때 "이미 다 지웠다"로 해석한 것이 첫 오판이었다. 정확한 해석은 "여기가 출처가 아니다"였고, 그 신호를 제대로 읽었다면 백엔드를 20분 먼저 봤을 것이다.

부수적으로, 프롬프트가 출력 **형식**을 텍스트로 강제하는 구조는 표현 계층을 LLM에 위임하는 것과 같다. 스키마로 데이터만 받고 표현은 UI가 정하게 바꾸면, 이모지 문제뿐 아니라 섹션별 렌더링·집계·액션 연동이 한꺼번에 열린다.

---

## TS-014 · 배포된 앱에서 로그인이 무한 대기 (콜드스타트 + 구버전 배포 + 환경변수 누락)

| | |
|---|---|
| **날짜** | 2026-08-05 |
| **영역** | Infra / Auth |
| **심각도** | High |
| **상태** | 해결됨 |
| **소요 시간** | 약 1시간 |

### 증상
그룹/인증 기능을 프론트엔드에 배포한 뒤, 실제 배포 주소에서 로그인을 시도하면 버튼이 "로그인 중..."에서 멈춘 채 아무 반응이 없었다. 브라우저 콘솔에는 에러가 없고, 네트워크 탭에도 백엔드 요청이 아예 잡히지 않았다.

### 재현 조건
- 환경: 프론트 Vercel(`onque-frontend.vercel.app`), 백엔드 Render 무료 티어
- 재현 절차: 15분 이상 유휴 상태 후 배포 주소에서 로그인 시도
- 재현율: 유휴 후 첫 요청에서 항상

### 원인
세 가지 문제가 겹쳐 있었고, 표면 증상은 하나였다.

- **표면**: "로그인이 안 된다" → 프론트엔드 인증 코드 버그로 오해하기 쉬움.
- **근본 1 (무응답의 직접 원인)**: Render 무료 인스턴스는 유휴 시 슬립되고, 깨어나는 데 실측 **54초**가 걸린다. 프론트에는 타임아웃도 안내 문구도 없어서 그냥 멈춘 것처럼 보였다.
- **근본 2 (실제로 로그인이 불가능했던 원인)**: 백엔드가 인증 기능 병합 **이전 버전**으로 떠 있었다. `openapi.json`을 직접 조회해보니 `/todos`, `/documents` 같은 구버전 라우트만 있고 `/api/v1/auth/login`은 아예 없었다(404).
- **근본 3 (재배포하면 터졌을 문제)**: `auth.py:15`가 `os.environ["JWT_SECRET"]`로 필수 조회하는데 `render.yaml`의 `envVars`에는 `GOOGLE_API_KEY`, `DATABASE_URL`만 선언돼 있었다. 그대로 재배포했다면 기동 즉시 `KeyError`로 죽었을 것이다. 배포 전에 코드를 훑다가 발견했다.

**확인 방법**: 서비스 존재 여부를 판별할 때, 존재하지 않는 Render 서브도메인은 즉시 404를 주는 반면 문제의 주소는 90초 대기 후 무응답(`status=000`)이었다. 이 차이로 "서비스가 삭제된 것이 아니라 기동 실패/슬립 상태"임을 구분했다.

```
# 존재하지 않는 주소
$ curl -s -o /dev/null -w "status=%{http_code}\n" https://onque-backend-nonexistent-xyz123.onrender.com/
status=404

# 문제의 주소 (라우팅은 살아있으나 응답 없음)
$ curl -s -o /dev/null -w "status=%{http_code} time=%{time_total}s\n" https://onque-backend-<REDACTED>.onrender.com/ --max-time 90
status=000 time=90.001938s
```

### 시도했지만 안 된 것
| 시도 | 결과 | 왜 안 됐는가 |
|---|---|---|
| 브라우저 콘솔·네트워크 탭 확인 | 단서 없음 | 요청이 pending 상태로 남아 있어 에러도 응답도 잡히지 않았다. 콜드스타트라는 사실을 모르면 여기서 막힌다 |
| `curl`로 백엔드 루트 호출 | 처음엔 1.4초 만에 200 응답 | 이미 깨어 있는 상태를 우연히 찔러서 "백엔드는 멀쩡하다"고 잘못 판단했다. 잠시 후 다시 호출하니 90초 무응답이 나와서야 슬립 동작을 인지 |
| Render 대시보드에서 서비스 찾기 | 사용자가 서비스를 못 찾음 | 실제로는 존재했다. 이 때문에 "서비스가 삭제됐다"는 잘못된 가설로 새 서비스 생성까지 준비했다 |

### 해결
1. **구버전 배포**: Render가 git push에 자동 반응하도록 되어 있어, 이후 커밋 푸시로 신버전이 배포됨. `openapi.json`에 `/api/v1/auth/login`, `/signup`, `/groups`, `/me`, `/announcements`가 나타난 것으로 확인.
2. **`JWT_SECRET` 누락**: `render.yaml`에 `generateValue: true`로 추가해 Render가 안전한 랜덤값을 자동 생성하도록 함. 비밀값을 저장소에 넣지 않으면서 필수 변수를 보장한다.
3. **콜드스타트 체감**: `useSlowRequestHint` 훅을 만들어 요청이 3초를 넘기면 로그인/회원가입 폼에 "서버를 깨우는 중입니다. 처음 접속은 1분까지 걸릴 수 있습니다." 안내를 띄우도록 함. 무료 티어를 유지하면서 멈춘 것으로 오해하지 않게 하는 선택.

추가로, 로그인에 성공해도 소속 그룹이 없어 모든 화면이 비는 문제가 드러나, 첫 가입자(admin)에게 기본 그룹을 자동 생성·가입시키는 온보딩을 함께 넣었다.

### 검증
- `curl`로 잘못된 자격증명 로그인 → `401` + `{"code":"AUTH_INVALID_CREDENTIALS"}` (봉투 형식 정상, DB 조회까지 도달 확인)
- 회원가입 → `role: admin`, `/api/v1/me`의 `groups`에 `기본 그룹` 자동 생성 확인
- 브라우저로 로그인 → 대시보드 진입, 사이드바 그룹 선택기에 `기본 그룹` 표시 확인
- 백엔드 테스트 36개 전부 통과 (자동 온보딩 테스트 2건 추가, 기존 테스트 1건은 바뀐 동작에 맞게 수정)

### 추후 관리
- **무료 티어 콜드스타트는 근본 해결이 아니라 안내로 우회한 상태다.** 포트폴리오 시연 직전에 미리 한 번 접속해 깨워두거나, 외부 cron으로 주기적 헬스체크를 걸거나, 유료 티어로 올리는 선택지가 남아 있다.
- 자동 온보딩은 **첫 가입자에게만** 적용된다. 두 번째 이후 가입자는 관리자가 초대해야 하는데 아직 그룹 관리 UI가 없어, 앱 안에서는 초대할 방법이 없다.
- `render.yaml`과 실제 코드가 요구하는 환경변수가 어긋나면 기동 즉시 죽는다. 새 필수 환경변수를 추가할 때 블루프린트도 함께 갱신해야 한다.

### 배운 점
- **한 번의 성공 응답으로 "서비스 정상"이라 결론내면 안 된다.** 슬립되는 무료 인스턴스는 깨어 있을 때와 잠들었을 때 완전히 다르게 행동한다. 최소 두 번, 간격을 두고 확인해야 한다.
- **"없다"와 "응답하지 않는다"는 다르다.** 존재하지 않는 리소스(즉시 404)와 기동 실패(타임아웃)를 구분하는 대조 실험이 잘못된 가설을 빨리 접게 해줬다.
- 배포 실패는 로그를 봐야 알 수 있지만, **배포 전에 코드가 요구하는 환경변수와 배포 설정을 대조하면 미리 잡을 수 있다.** `grep`으로 `os.environ`/`getenv`를 훑는 30초가 실패한 배포 한 번보다 싸다.

---

## TS-013 · pytest용 SQLite 인메모리 DB가 FastAPI 워커 스레드에서 `no such table` 에러

| | |
|---|---|
| **날짜** | 2026-08-05 |
| **영역** | BE (테스트 인프라) |
| **심각도** | Medium |
| **상태** | 해결됨 |
| **소요 시간** | 약 30분 (구현·리뷰·수정 라운드 포함) |

### 증상
그룹/인증 구조 도입 작업의 Task 1(pytest + SQLite 인메모리 테스트 인프라)에서, `conftest.py`의 `client` fixture로 DB를 실제로 건드리지 않는 라우트(`GET /`)를 테스트할 때는 통과했지만, DB를 쓰는 라우트(`GET /todos`)를 테스트하는 순간 다음 에러로 실패했다.

```
sqlalchemy.exc.OperationalError: (sqlite3.OperationalError) no such table: todos
```

### 재현 조건
- 환경: `create_engine("sqlite:///:memory:", connect_args={"check_same_thread": False})`로 만든 SQLite 엔진에 `poolclass`를 지정하지 않음
- 재현 절차: fixture 함수(메인 스레드)에서 `Base.metadata.create_all(bind=TEST_ENGINE)` 실행 → FastAPI `TestClient`로 동기 `Depends(get_db)` 라우트 호출
- 재현율: 항상. DB를 쓰지 않는 라우트(`GET /`)는 절대 이 문제를 드러내지 않아, 애초에 검증용으로 고른 스모크 테스트(`test_health_check`)가 문제를 못 잡고 통과시켰다.

### 원인
- **표면**: "테이블이 없다"는 메시지라 스키마 생성(`create_all`)을 빼먹은 줄 알기 쉽다.
- **근본**: SQLite `:memory:` 엔진의 기본 커넥션 풀은 `SingletonThreadPool`로, 스레드마다 별도의 인메모리 DB를 새로 만든다. FastAPI는 동기(`def`, 비-`async def`) 의존성인 `get_db`를 `run_in_threadpool`로 별도 워커 스레드에서 실행하는데, `create_all()`은 fixture가 실행되는 메인 스레드에서 돌기 때문에 워커 스레드는 매번 텅 빈 새 DB를 보게 된다. 모든 스레드가 같은 커넥션(=같은 인메모리 DB)을 공유하게 하려면 `poolclass=StaticPool`이 필요하다 — SQLite+FastAPI 테스트 조합에서 잘 알려진 함정.
- **확인 방법**: 코드 리뷰 서브에이전트가 동일 조건(SQLite `:memory:` 엔진 + 메인 스레드 `create_all` + 동기 `Depends(get_db)` 라우트 + `TestClient`)을 별도 스크립트로 직접 재현해 정확히 같은 에러를 재확인했다.

### 시도했지만 안 된 것
| 시도 | 결과 | 왜 안 됐는가 |
|---|---|---|
| (없음) | — | 애초 작성한 계획 문서의 `conftest.py` 코드 자체에 `poolclass` 누락이 있었고, 구현 직후 코드 리뷰 단계에서 바로 걸러져 별도 시행착오 없이 원인이 특정됨 |

### 해결
`TEST_ENGINE` 생성 코드에 `from sqlalchemy.pool import StaticPool`을 추가하고 `create_engine(..., poolclass=StaticPool)`로 수정. 재발 방지로, fixture 검증용 테스트에 DB를 실제로 쓰는 라우트(`GET /todos`) 호출을 추가했다 — 기존엔 DB 미사용 라우트만 테스트해서 이 버그를 놓쳤었다.

### 검증
`StaticPool` 적용 후 `GET /todos`를 호출하는 신규 테스트를 포함해 전체 pytest 스위트가 통과했고, 이후 이어진 태스크들의 DB 의존 테스트(회원가입/로그인/그룹 등)도 모두 정상 동작했다.

### 추후 관리
- **재발 방지**: 앞으로 SQLite 인메모리 엔진으로 FastAPI 테스트 fixture를 만들 때는 `poolclass=StaticPool`을 기본값으로 넣는다.
- **모니터링**: 해당 없음(테스트 전용 이슈).
- **남은 리스크**: fixture 스모크 테스트를 짤 때 DB 미사용 라우트만 고르면 이런 종류의 버그를 계속 놓칠 수 있다 — 최소 하나는 DB 의존 라우트를 포함시키는 습관이 필요.

## TS-012 · 프로젝트 폴더 이동 후 venv 셔뱅 경로가 깨져 `ModuleNotFoundError`

| | |
|---|---|
| **날짜** | 2026-08-05 |
| **영역** | Infra |
| **심각도** | Medium |
| **상태** | 해결됨 |
| **소요 시간** | 약 10분 |

### 증상
로컬에서 앱을 시연하려고 백엔드(`uvicorn main:app`)를 실행하자 곧바로 다음 에러로 죽었다.

```
ModuleNotFoundError: No module named 'sqlalchemy'
```

`sqlalchemy`는 `requirements.txt`에 명시돼 있고 이전에 정상 설치되어 있던 패키지라 의아했다.

### 재현 조건
- 환경: 프로젝트 폴더가 원래 `~/Desktop/OnQue`였다가 이후 `~/Project/OnQue`로 이동됨. `venv/`는 그 이동 전에 `~/Desktop/OnQue`에서 생성된 것을 그대로 가지고 옴.
- 재현 절차: 이동된 폴더에서 `source venv/bin/activate && uvicorn ...` 또는 `./venv/bin/python -m uvicorn ...` 실행
- 재현율: 항상 (venv를 재생성하기 전까지)

### 원인
- **표면**: `sqlalchemy` import 실패
- **근본**: `venv/bin/uvicorn`, `venv/bin/pip` 등 venv 내부의 실행 스크립트들은 셔뱅(shebang) 줄에 venv 생성 당시의 절대 경로를 그대로 박아둔다(`#!/Users/tina/Desktop/OnQue/venv/bin/python3.12`). 폴더가 `~/Project/OnQue`로 이동되면서 그 경로의 `python3.12` 실행파일 자체가 더는 존재하지 않게 됐고, `uvicorn` 실행 시 셸이 시스템 전역 Python(3.13)으로 조용히 폴백해 venv에 설치된 패키지를 전혀 못 찾는 상태가 됐다.
- **확인 방법**: `./venv/bin/pip show sqlalchemy` 실행 시 `bad interpreter: /Users/tina/Desktop/OnQue/venv/bin/python3.12: no such file or directory` 에러로 셔뱅 경로 문제를 직접 확인.

### 시도했지만 안 된 것
| 시도 | 결과 | 왜 안 됐는가 |
|---|---|---|
| `source venv/bin/activate` 후 재실행 | 동일하게 실패 | activate 스크립트도 결국 셔뱅이 깨진 인터프리터를 가리켜, PATH만 바뀔 뿐 실제 실행 파일 문제는 해결 안 됨 |

### 해결
`rm -rf venv && python3 -m venv venv && ./venv/bin/pip install -r requirements.txt`로 현재 경로(`~/Project/OnQue`) 기준으로 venv를 완전히 재생성.

### 검증
`./venv/bin/python -m uvicorn main:app --host 0.0.0.0 --port 8001` 정상 기동, `curl localhost:8001/docs` 200 응답 확인.

### 추후 관리
- **재발 방지**: 프로젝트 폴더를 다시 옮기게 되면 venv는 절대 같이 옮기지 말고 항상 새 위치에서 재생성한다. venv를 `.gitignore`에 유지하는 것도 이 때문에 중요 (재현 가능한 산출물로 취급).
- **모니터링**: 해당 없음.
- **남은 리스크**: 없음 — 원인이 명확하고 재생성으로 완전히 해소됨.

## TS-011 · Vercel이 GitHub push에 자동 반응하지 않아 백엔드만 재배포되고 프론트는 구버전으로 남음

| | |
|---|---|
| **날짜** | 2026-08-04 |
| **영역** | Infra |
| **심각도** | Medium |
| **상태** | 해결됨 |
| **소요 시간** | 약 5분 |

### 증상
DB 연동 기능을 `git push`한 뒤 Render 백엔드는 자동 재배포되어 `/todos` 등 신규 엔드포인트가 정상 응답했지만, 같은 시점에 Vercel 프론트엔드(`https://onque-frontend.vercel.app`)에 접속하면 새로 추가한 `/chat` 등 경로가 반영되지 않은 상태였다.

### 재현 조건
- 환경: GitHub 저장소(`jjssspark/OnQue`) → Render(Blueprint 자동 배포 연동) / Vercel(초기에 `npx vercel` CLI로 device-code 로그인 후 수동 배포)
- 재현 절차: 코드 변경 → `git push origin main` → Render는 몇 분 내 자동 재배포, Vercel은 아무 변화 없음
- 재현율: 항상 (Vercel 프로젝트가 GitHub App으로 연결된 게 아니라 CLI로 최초 배포됐기 때문)

### 원인
- **표면**: 백엔드는 최신인데 프론트엔드만 예전 버전으로 남아 있음
- **근본**: 이 프로젝트의 Vercel 배포는 초기 설정 시 GitHub 저장소 연동(웹훅 기반 자동 배포)이 아니라 `npx vercel --prod --yes` CLI 명령으로 직접 배포했다. Render는 `render.yaml` Blueprint가 GitHub push를 감지해 자동 재배포하지만, Vercel 쪽은 그런 트리거가 걸려 있지 않아 코드를 아무리 push해도 그 자체로는 재배포되지 않는다.
- **확인 방법**: `curl -s -o /dev/null -w "%{http_code}" https://onque-frontend.vercel.app/documents` 등으로 신규 라우트를 직접 호출해 404를 확인. Render 쪽 동일 점검(`/openapi.json`에 새 경로 포함 여부)은 정상이라 두 배포 파이프라인의 동작 차이임을 특정.

### 시도했지만 안 된 것
| 시도 | 결과 | 왜 안 됐는가 |
|---|---|---|
| git push 후 그대로 대기 | 프론트 반영 안 됨 | Vercel 프로젝트가 GitHub 웹훅에 연결돼 있지 않음 |

### 해결
`onque-frontend/`에서 `npx vercel --prod --yes`를 수동으로 재실행해 최신 커밋 기준으로 프로덕션에 재배포.

### 검증
`curl -s -o /dev/null -w "%{http_code}\n" https://onque-frontend.vercel.app/chat` 등으로 200 확인, 브라우저로 대시보드 접속해 실제 DB 데이터(할 일·일정·이력)가 렌더링되는 것까지 최종 확인.

### 추후 관리
- **재발 방지**: 없음(수동 배포 절차를 알고 있는 상태로 진행). Vercel 대시보드에서 GitHub App 연동을 켜면 이후 `git push`만으로 자동 배포되도록 개선 가능 — 사용자에게 안내함.
- **모니터링**: 없음.
- **남은 리스크**: 앞으로도 코드 변경 후 `git push`만 하고 Vercel 수동 배포를 잊으면 같은 문제가 재발한다.
- **후속 작업**: (선택) Vercel 프로젝트 설정에서 GitHub 저장소 연동 활성화.

### 배운 점
"같은 GitHub 저장소를 보고 있다"는 사실만으로 모든 배포 플랫폼이 동일하게 자동 배포된다고 가정하면 안 된다. 각 플랫폼이 *어떤 방식으로* 연결됐는지(웹훅 기반 연동 vs. CLI 수동 배포)를 배포 초기에 확인해두지 않으면, "푸시했는데 왜 안 바뀌지"를 매번 새로 진단해야 한다.

### 참고
- 없음

---

## TS-010 · Gemini 무료 티어 일시 과부하(503)로 `@비서` 봇 답변이 폴백 메시지로 나감

| | |
|---|---|
| **날짜** | 2026-08-04 |
| **영역** | 외부API |
| **심각도** | Low |
| **상태** | 해결됨 (재시도로 해소, 코드 변경 없음) |
| **소요 시간** | 약 15분 (원인 특정 포함) |

### 증상
팀 채팅에서 `@비서 오늘 할 일 뭐 있어?`처럼 멘션했을 때, 메시지 자체는 정상 저장되고 할 일/일정 자동 추출도 잘 되는데 봇 답변만 아래처럼 고정 폴백 문구로 나왔다.

```
죄송해요, 지금은 답변을 생성하지 못했어요.
```

### 재현 조건
- 환경: `gemini_service.generate_bot_reply()`, 모델 `gemini-2.5-flash`
- 재현 절차: `/chat/messages`에 `@비서`가 포함된 메시지 POST
- 재현율: 간헐적 (같은 프롬프트를 몇 초 뒤 재시도하면 정상 응답)

### 원인
- **표면**: 봇 답변 생성 함수가 예외를 삼키고 고정 폴백 문자열을 반환.
- **근본**: `generate_bot_reply()` 내부 `client.models.generate_content()` 호출이 Google 서버로부터 `503 UNAVAILABLE`을 받았음. 같은 프롬프트를 바로 재시도하면 정상 응답이 오는 것으로 보아 코드 결함이 아니라 Gemini API 쪽의 순간적 트래픽 과부하였다.
- **확인 방법**: `try/except`로 감춰진 실제 예외를 보려고 함수 내부 로직을 그대로 복제해 직접 실행, `traceback.print_exc()`로 전체 스택 확인:
  ```
  google.genai.errors.ServerError: 503 UNAVAILABLE. {'error': {'code': 503, 'message':
  'This model is currently experiencing high demand. Spikes in demand are usually
  temporary. Please try again later.', 'status': 'UNAVAILABLE'}}
  ```
  직후 동일 호출을 재실행하니 정상 응답(`response.candidates`에 실제 텍스트 포함)을 받아, 일시적 과부하였음을 확인.

### 시도했지만 안 된 것
| 시도 | 결과 | 왜 안 됐는가 |
|---|---|---|
| (없음) | — | 전체 스택트레이스에서 `503 UNAVAILABLE`이 바로 드러나 다른 가설을 세울 필요가 없었다 |

### 해결
코드 변경 없음. `gemini_service.py`의 `generate_bot_reply()`는 이미 예외 발생 시 사용자에게 자연스러운 한국어 폴백 메시지("죄송해요, 지금은 답변을 생성하지 못했어요.")를 보여주도록 설계돼 있어, 이 상황 자체는 UX상 허용 가능한 정상 동작으로 판단하고 그대로 두었다.

### 검증
같은 프롬프트로 몇 초 뒤 재호출 → 정상 텍스트 응답 확인. 이후 브라우저 e2e 테스트에서도 대부분의 호출은 정상 응답했다.

### 추후 관리
- **재발 방지**: 없음(외부 API 가용성 문제라 애플리케이션 코드로 근본 해결 불가).
- **모니터링**: 없음. 필요하면 재시도 로직(지수 백오프 1~2회)을 추가해 사용자 체감 실패율을 낮출 수 있음 — 미적용.
- **남은 리스크**: Gemini API가 과부하 상태일 때는 여전히 폴백 메시지가 노출된다.
- **후속 작업**: (선택) `generate_bot_reply()`에 짧은 재시도(예: 1회, 1~2초 대기) 추가 검토.

### 배운 점
`except Exception`으로 폭넓게 잡아 사용자 친화적 폴백을 보여주는 설계는 안정성 면에서 옳지만, 그 대가로 "코드 버그"와 "외부 서비스 일시 장애"가 로그상 구분되지 않는다. 폴백이 반복될 때는 항상 `except` 블록을 우회해 실제 예외 타입(여기서는 `google.genai.errors.ServerError`)을 직접 확인하는 것이 원인 오판을 막는 가장 빠른 방법이었다.

### 참고
- 없음

---

## TS-009 · SQLAlchemy가 `postgresql://` 스킴에서 기본으로 psycopg2를 찾는데 psycopg3만 설치되어 있어 `ModuleNotFoundError`

| | |
|---|---|
| **날짜** | 2026-08-04 |
| **영역** | DB |
| **심각도** | Medium |
| **상태** | 해결됨 |
| **소요 시간** | 약 10분 |

### 증상
`DATABASE_URL`을 정상적으로 읽도록 고친 뒤(TS-007, TS-008 해결 후) `main.py`를 임포트하면 다음 에러로 죽었다.

```
File ".../sqlalchemy/dialects/postgresql/psycopg2.py", line 696, in import_dbapi
    import psycopg2
ModuleNotFoundError: No module named 'psycopg2'
```

### 재현 조건
- 환경: `requirements.txt`에 `psycopg[binary]==3.2.10`(psycopg **3**)만 명시, Neon이 발급한 connection string은 `postgresql://user:pass@host/db?sslmode=require` 형태(스킴에 드라이버 명시 없음)
- 재현 절차: `python -c "import main"`
- 재현율: 항상

### 원인
- **표면**: `psycopg2`라는, 애초에 설치한 적 없는 패키지를 찾다가 실패.
- **근본**: SQLAlchemy는 URL 스킴이 `postgresql://`(드라이버 미지정)이면 **기본값으로 psycopg2용 드라이버**를 로드하려 시도한다. 이 프로젝트는 최신 유지보수 라인인 psycopg3(`psycopg[binary]`)를 설치했는데, SQLAlchemy에게 "psycopg3를 쓰라"고 명시하지 않아 기본 동작(psycopg2 시도)과 실제 설치된 패키지가 어긋났다.
- **확인 방법**: 트레이스백이 `sqlalchemy/dialects/postgresql/psycopg2.py`를 직접 가리켜 원인이 즉시 드러났다.

### 시도했지만 안 된 것
| 시도 | 결과 | 왜 안 됐는가 |
|---|---|---|
| (없음) | — | 에러 메시지가 원인을 명확히 지목해 바로 수정 |

### 해결
`db.py`에서 `postgresql://`로 시작하는 URL을 `postgresql+psycopg://`(psycopg3 드라이버 명시)로 변환한 뒤 엔진을 생성하도록 수정.

```python
# psycopg(v3) 드라이버를 쓰도록 스킴을 명시한다. Neon 등이 주는 기본
# `postgresql://`는 SQLAlchemy가 psycopg2를 찾게 만든다.
if DATABASE_URL.startswith("postgresql://"):
    DATABASE_URL = DATABASE_URL.replace("postgresql://", "postgresql+psycopg://", 1)

engine = create_engine(DATABASE_URL, pool_pre_ping=True)
```

### 검증
`python -c "import main; print(sorted(r.path for r in main.app.routes if hasattr(r,'path')))"` 실행 → 에러 없이 전체 라우트 목록 출력, DB 테이블 생성(`Base.metadata.create_all`)까지 정상 완료.

### 추후 관리
- **재발 방지**: `requirements.txt`에 psycopg3를 쓰기로 한 이상, `DATABASE_URL`을 다루는 코드는 항상 스킴을 `+psycopg`로 명시하는 규칙을 `db.py` 안에 코드로 강제해뒀다(사람이 URL 형식을 매번 기억할 필요 없음).
- **모니터링**: 없음.
- **남은 리스크**: 없음.
- **후속 작업**: 없음.

### 배운 점
DB 드라이버를 pip로 설치하는 것과, ORM(SQLAlchemy)이 그 드라이버를 실제로 사용하도록 커넥션 문자열에 명시하는 것은 별개의 단계다. 클라우드 DB 서비스(Neon, Supabase 등)가 주는 기본 `postgresql://` connection string은 드라이버에 대해 중립적이므로, 어떤 드라이버를 설치했는지에 따라 애플리케이션 코드에서 스킴을 보정해줘야 한다.

### 참고
- SQLAlchemy PostgreSQL 드라이버 문서: <https://docs.sqlalchemy.org/en/20/dialects/postgresql.html>

---

## TS-008 · `.env`의 `DATABASE_URL` 키 뒤 공백 + 키 없는 값만 있는 빈 줄이 섞여 환경변수 인식 실패

| | |
|---|---|
| **날짜** | 2026-08-04 |
| **영역** | Infra |
| **심각도** | Medium |
| **상태** | 해결됨 |
| **소요 시간** | 약 10분 |

### 증상
사용자가 Neon에서 발급받은 connection string을 `.env`에 추가했다고 확인했는데("했어"), `grep -q "^DATABASE_URL=" .env`로 확인하면 계속 "아직 없음"으로 나왔다.

### 재현 조건
- 환경: `/Users/tina/Project/OnQue/.env`
- 재현 절차: `grep -E "^DATABASE_URL="  .env` 실행
- 재현율: 항상 (파일 내용이 그렇게 저장돼 있었으므로)

### 원인
- **표면**: `DATABASE_URL=` 로 시작하는 줄이 없다고 grep이 보고.
- **근본**: 값 자체는 레딕트해 확인한 결과, ①`DATABASE_URL` 키 뒤에 공백이 붙어 `DATABASE_URL =...` 형태였고(`=` 앞 공백 때문에 `^DATABASE_URL=` 패턴에 안 걸림), ②그와 별개로 키 없이 값만 있는(`=<값>`) 정체불명의 빈 줄이 하나 더 섞여 있었다. 두 문제 모두 `.env`를 편집하는 과정에서 붙여넣기 실수로 생긴 것으로 추정(직접 확인은 안 함 — 추정).
- **확인 방법**: 비밀값을 노출하지 않기 위해 `sed -E 's/=.*/=<REDACTED>/' .env | cat -e`로 값은 가리고 줄 구조만 확인:
  ```
  GOOGLE_API_KEY=<REDACTED>$
  $
  =<REDACTED>$
  $
  DATABASE_URL=<REDACTED>
  ```
  (수정 전에는 `DATABASE_URL` 앞에 공백이 있었고, 3번째 줄이 `=<REDACTED>`로 키가 비어 있었다.)

### 시도했지만 안 된 것
| 시도 | 결과 | 왜 안 됐는가 |
|---|---|---|
| `grep -q "^DATABASE_URL="` 재확인만 반복 | 계속 "없음" | grep 패턴 자체는 맞았고, 문제는 파일 내용 쪽이라 재확인만으로는 못 찾음. 실제 파일 구조를 값 레딕트 후 직접 들여다봐야 했다 |

### 해결
값은 절대 화면에 노출하지 않고 `sed`로 키 이름·구조만 교정:

```bash
# 1) 키 이름의 트레일링 공백 제거: "DATABASE_URL =" → "DATABASE_URL="
sed -i.bak -E 's/^DATABASE_URL[[:space:]]*=/DATABASE_URL=/' .env && rm .env.bak

# 2) 키 없이 "=값"만 있는 빈 줄 삭제
sed -i.bak '/^=/d' .env && rm .env.bak
```

### 검증
`grep -q "^DATABASE_URL=" .env && echo 설정됨` → "설정됨" 출력. 이후 `python -c "import main"`으로 실제 DB 연결까지 성공(TS-009로 이어짐)해 최종 확인.

### 추후 관리
- **재발 방지**: `.env.example`을 새로 만들어(`GOOGLE_API_KEY`, `DATABASE_URL` 키 목록만 문서화) 이후 값을 추가할 때 형식을 참고할 수 있게 함.
- **모니터링**: 없음.
- **남은 리스크**: 사람이 `.env`를 손으로 편집하는 한 같은 유형의 오타는 재발할 수 있음.
- **후속 작업**: 없음.

### 배운 점
환경변수가 "설정 안 됨"으로 읽힐 때는 값 자체보다 **줄 구조(키 이름의 공백, 빈 줄, 인코딩)** 를 먼저 의심해야 한다. 특히 비밀값이 섞인 파일을 진단할 때는 `sed`로 값만 레딕트하고 키/구조만 노출하는 방식을 쓰면, 실제 값을 노출하지 않고도 안전하게 원인을 좁힐 수 있다.

### 참고
- 없음

---

## TS-007 · `db.py`가 `.env`를 직접 로드하지 않아 import 순서에 따라 `DATABASE_URL`을 못 찾고 기동 실패

| | |
|---|---|
| **날짜** | 2026-08-04 |
| **영역** | BE |
| **심각도** | Medium |
| **상태** | 해결됨 |
| **소요 시간** | 약 5분 |

### 증상
`DATABASE_URL`을 `.env`에 넣은 직후(TS-008 수정 전 상태) `python -c "import main"`을 실행하면 다음 에러로 즉시 죽었다.

```
File "/Users/tina/Project/OnQue/main.py", line 10, in <module>
    import gemini_service
File "/Users/tina/Project/OnQue/gemini_service.py", line 12, in <module>
    from models import DOCUMENT_CATEGORIES
File "/Users/tina/Project/OnQue/models.py", line 6, in <module>
    from db import Base
File "/Users/tina/Project/OnQue/db.py", line 9, in <module>
    raise RuntimeError("DATABASE_URL 환경변수가 설정되어 있지 않습니다.")
RuntimeError: DATABASE_URL 환경변수가 설정되어 있지 않습니다.
```

### 재현 조건
- 환경: `main.py` → `gemini_service.py`(내부에서 `load_dotenv()` 호출) → `models.py` → `db.py`(`os.getenv("DATABASE_URL")`만 호출, `load_dotenv()` 없음) import 체인
- 재현 절차: `python -c "import main"`
- 재현율: 항상 (모듈이 이 순서로 임포트되는 한)

### 원인
- **표면**: `.env`에 값을 넣었는데도 "설정 안 됨" 에러.
- **근본**: `.env` 로딩(`load_dotenv()`)이 `gemini_service.py`에만 있었다. Python import 체인상 `db.py`가 `gemini_service.py`보다 **먼저** 평가되는데(정확히는 `models.py`가 `db.py`를 import하는 시점에 `db.py` 최상단 코드가 실행됨), 이 시점엔 아직 `load_dotenv()`가 호출되지 않아 프로세스 환경변수에 `.env` 내용이 반영돼 있지 않았다.
- **확인 방법**: 트레이스백의 import 체인을 그대로 읽으면 `db.py`가 가장 먼저 죽는 지점이라는 게 바로 드러남.

### 시도했지만 안 된 것
| 시도 | 결과 | 왜 안 됐는가 |
|---|---|---|
| (없음) | — | 트레이스백만으로 원인이 명확했다 |

### 해결
`db.py`가 다른 모듈의 로딩 순서에 의존하지 않도록, 자체적으로 `.env`를 로드하게 수정.

```python
# 수정 전
import os
from sqlalchemy import create_engine
...
DATABASE_URL = os.getenv("DATABASE_URL")

# 수정 후
import os
from dotenv import load_dotenv
from sqlalchemy import create_engine
...
load_dotenv()
DATABASE_URL = os.getenv("DATABASE_URL")
```

### 검증
`python -c "import main"` 재실행 → 이 에러는 사라지고 다음 문제(TS-009, psycopg 드라이버)로 넘어감. 최종적으로 전체 임포트 성공 확인.

### 추후 관리
- **재발 방지**: 환경변수를 읽는 모든 진입점 모듈(`db.py`, `gemini_service.py`)이 각자 `load_dotenv()`를 호출하도록 함 — 어느 모듈이 먼저 임포트되든 안전.
- **모니터링**: 없음.
- **남은 리스크**: 없음.
- **후속 작업**: 없음.

### 배운 점
여러 모듈에 걸쳐 환경변수를 읽을 때 `load_dotenv()`를 "진입점 한 곳에서만" 호출하면, 그 진입점보다 먼저 평가되는 다른 모듈에서는 환경변수가 비어 있을 수 있다. `python-dotenv`의 `load_dotenv()`는 여러 번 호출해도 안전(idempotent)하므로, 환경변수에 의존하는 모듈은 각자 방어적으로 호출하는 편이 import 순서에 안전하다.

### 참고
- python-dotenv 문서: <https://saurabh-kumar.com/python-dotenv/>

---

## TS-006 · 이력 페이지가 SSR에서는 빈 상태, 클라이언트에서는 즉시 localStorage 값으로 렌더링돼 hydration mismatch 발생

| | |
|---|---|
| **날짜** | 2026-08-04 |
| **영역** | FE |
| **심각도** | Medium |
| **상태** | 해결됨 |
| **소요 시간** | 약 10분 |

### 증상
UI를 업무 플랫폼 셸로 재구성한 직후, `/history` 페이지에서 화면 좌하단에 Next.js dev 오버레이가 "1 Issue" 배지를 띄웠다. 열어보면:

```
Recoverable Error
Hydration failed because the server rendered HTML didn't match the client.
...
+       className="rounded-xl border border-border bg-surface shadow-sm"
-       className="rounded-xl border border-dashed border-border p-10 text-center text-sm text-...
...
app/history/page.tsx (74:15) @ <unknown>
> 74 |     <button
```

### 재현 조건
- 환경: Next.js 16.3.0(App Router), `app/history/page.tsx`가 `'use client'` 컴포넌트, `useMemo(() => searchHistory(...), [query, typeFilter])`로 브라우저 `localStorage`에서 이력을 즉시 계산
- 재현 절차: `/history` 페이지 새로고침(서버 렌더링 + 클라이언트 hydration이 모두 일어나는 시점)
- 재현율: 항상 (localStorage에 이미 저장된 이력이 있는 상태에서)

### 원인
- **표면**: 서버가 만든 HTML과 클라이언트가 다시 그린 HTML이 달라 React가 hydration을 포기하고 해당 트리를 통째로 재생성.
- **근본**: `'use client'` 컴포넌트도 최초 진입 시 서버에서 한 번 렌더링(SSR)된다. 서버에는 `window`/`localStorage`가 없으므로 `lib/history.ts`의 `readAll()`이 빈 배열을 반환 → 서버가 만드는 HTML은 "이력 없음" 빈 상태(점선 테두리 카드)였다. 반면 클라이언트에서는 `useMemo`가 렌더링 시점에 즉시 `localStorage`를 읽어 실제 이력 목록(카드 리스트)을 그렸다. 이 두 결과물이 첫 페인트에서 서로 달라 hydration mismatch가 났다.
- **확인 방법**: Next.js dev 오버레이가 정확한 파일·줄(`app/history/page.tsx (74:15)`)과 diff를 직접 보여줘 원인이 바로 드러남.

### 시도했지만 안 된 것
| 시도 | 결과 | 왜 안 됐는가 |
|---|---|---|
| (없음) | — | 에러 메시지에 정확한 원인과 위치가 이미 담겨 있었다 |

### 해결
`useMemo`로 렌더링 중 즉시 계산하던 것을, `useState` 초기값을 빈 배열로 두고 `useEffect`(마운트 이후, 클라이언트에서만 실행)에서 채우는 방식으로 변경. 서버와 클라이언트의 **첫 렌더링 결과를 동일하게(빈 상태)** 맞춘 뒤, mount 이후에만 실제 데이터로 갱신되도록 함.

```tsx
// 수정 전
const entries = useMemo(
  () => searchHistory(query, typeFilter === 'all' ? undefined : typeFilter),
  [query, typeFilter]
);

// 수정 후
const [entries, setEntries] = useState<HistoryEntry[]>([]);
useEffect(() => {
  setEntries(searchHistory(query, typeFilter === 'all' ? undefined : typeFilter));
}, [query, typeFilter]);
```

### 검증
페이지 재접속 후 화면 좌하단 이슈 배지가 사라짐을 확인. 이력 목록도 정상적으로 표시되고, 항목 펼치기·검색·타입 필터 모두 정상 동작 확인.

### 추후 관리
- **재발 방지**: 이후 만든 `RecentActivity.tsx` 등 브라우저 저장소(당시 localStorage, 이후 백엔드 API)에 의존하는 컴포넌트는 처음부터 "초기값 없음 → `useEffect`에서 채움" 패턴으로 작성.
- **모니터링**: 없음(개발 환경 dev 오버레이가 사실상의 모니터링 역할).
- **남은 리스크**: 없음.
- **후속 작업**: 없음 — 이후 이 페이지 자체가 DB 기반(`/documents` API)으로 전환되며 localStorage 의존은 완전히 제거됨.

### 배운 점
Next.js의 `'use client'`는 "이 컴포넌트는 클라이언트에서도 상호작용 가능하다"는 뜻이지 "서버에서 렌더링 안 한다"는 뜻이 아니다. `window`/`localStorage`처럼 서버에 없는 브라우저 전용 값을 렌더링 중(useMemo, 함수 본문)에 직접 읽으면, 서버와 클라이언트의 첫 결과물이 갈라져 hydration mismatch가 난다. 브라우저 전용 데이터는 항상 "빈 초기값 → `useEffect`에서 채움" 패턴으로 서버/클라이언트 첫 렌더링을 일치시켜야 한다.

### 참고
- Next.js hydration 에러 문서: <https://nextjs.org/docs/messages/react-hydration-error>

---

## TS-005 · 서버 컴포넌트에서 함수를 클라이언트 컴포넌트 props로 전달해 Next.js 빌드가 `/calls`에서 실패

| | |
|---|---|
| **날짜** | 2026-08-04 |
| **영역** | FE/Build |
| **심각도** | High |
| **상태** | 해결됨 |
| **소요 시간** | 약 10분 |

### 증상
UI를 3단 레이아웃으로 재구성하면서 통화/문서 요약 업로드 UI를 `UploadPanel` 공용 컴포넌트로 뽑고, `app/calls/page.tsx`(서버 컴포넌트)에서 `onSubmit={summarizeCall}`처럼 함수를 props로 넘겼더니 프로덕션 빌드가 실패했다.

```
Error occurred prerendering page "/calls". Read more: https://nextjs.org/docs/messages/prerender-error
Error: Event handlers cannot be passed to Client Component props.
  {accept: ..., acceptHint: ..., historyType: ..., submitLabel: ..., loadingLabel: ..., loadingHint: ..., emptySelectionMessage: ..., onSubmit: function}
                                                                                                                                                ^^^^^^^^
If you need interactivity, consider converting part of this to a Client Component.
    at ignore-listed frames {
  digest: '3398921023'
}
Export encountered an error on /calls/page: /calls, exiting the build.
⨯ Next.js build worker exited with code: 1 and signal: null
```

### 재현 조건
- 환경: Next.js 16.3.0 App Router, `app/calls/page.tsx`는 `'use client'` 없는 기본 서버 컴포넌트, `components/UploadPanel.tsx`는 `'use client'` 컴포넌트
- 재현 절차: `app/calls/page.tsx`에서 `lib/api.ts`의 `summarizeCall` 함수를 import해 `<UploadPanel onSubmit={summarizeCall} .../>`로 전달 후 `npm run build`
- 재현율: 항상

### 원인
- **표면**: 빌드가 `/calls` 정적 페이지 생성 단계에서 예외로 중단.
- **근본**: React Server Components 모델에서 서버 컴포넌트가 클라이언트 컴포넌트에 넘기는 props는 **직렬화 가능한 값**이어야 한다. 함수(이벤트 핸들러 포함)는 서버→클라이언트 경계를 건널 수 없다. `summarizeCall`은 일반 함수이지만 Next.js 입장에서는 "직렬화 불가능한 값을 클라이언트 컴포넌트 props로 넘긴 것"과 동일하게 취급돼 빌드 타임에 에러가 난다.
- **확인 방법**: 에러 메시지가 정확히 어떤 prop(`onSubmit: function`)이 문제인지, 어떤 페이지(`/calls`)에서 발생했는지까지 명시해 원인이 즉시 드러남.

### 시도했지만 안 된 것
| 시도 | 결과 | 왜 안 됐는가 |
|---|---|---|
| (없음) | — | 에러 메시지가 원인과 해법 방향("Client Component로 바꾸는 걸 고려하라")까지 제시해 바로 수정 |

### 해결
`page.tsx`를 클라이언트 컴포넌트로 바꾸는 대신(서버 컴포넌트 이점을 유지하기 위해), `UploadPanel`이 함수를 props로 받지 않고 `historyType`(`'call' | 'document'`) 문자열만 받아 내부에서 어떤 API 함수를 호출할지 스스로 선택하도록 리팩터링.

```tsx
// components/UploadPanel.tsx — 수정 후
const SUMMARIZE_FN: Record<UploadKind, (file: File) => Promise<SummaryResponse>> = {
  call: summarizeCall,
  document: summarizeDocument,
};
// ...
const data = await SUMMARIZE_FN[historyType](file);
```

```tsx
// app/calls/page.tsx — onSubmit prop 제거
<UploadPanel
  accept=".mp3,.m4a,.wav"
  historyType="call"
  // onSubmit={summarizeCall}  ← 제거
  ...
/>
```

### 검증
`npm run build` 재실행 → `/calls`, `/documents`, `/chat`, `/history` 등 전 라우트가 정적 페이지로 정상 생성됨을 확인.

### 추후 관리
- **재발 방지**: 서버 컴포넌트에서 클라이언트 컴포넌트로 "동작"을 전달해야 할 때는 함수 prop 대신 문자열/enum 같은 식별자를 넘기고, 실제 함수 매핑은 클라이언트 컴포넌트 내부(또는 별도 client-only 모듈)에 두는 패턴을 이후에도 계속 사용.
- **모니터링**: 없음(빌드 타임에 항상 걸러짐).
- **남은 리스크**: 없음.
- **후속 작업**: 없음.

### 배운 점
Next.js App Router에서 "서버 컴포넌트가 기본값"이라는 점은 개발 중 종종 잊기 쉽다. 클라이언트 컴포넌트에 재사용 가능한 로직을 주입하고 싶을 때 가장 먼저 드는 생각(콜백 함수를 prop으로 넘기기)이 바로 이 아키텍처에서는 막혀 있다는 것을, 빌드 에러가 아니라 설계 시점에 미리 인지하고 있어야 반복적인 리팩터링을 줄일 수 있다.

### 참고
- Next.js Server/Client Component 경계 문서: <https://nextjs.org/docs/app/building-your-application/rendering/composition-patterns>

---

## TS-004 · Next.js 16.0.3의 critical RCE(CVSS 10) 등 취약점으로 Vercel이 배포를 차단

| | |
|---|---|
| **날짜** | 2026-08-04 |
| **영역** | Build/Infra |
| **심각도** | Critical |
| **상태** | 해결됨 |
| **소요 시간** | 약 15분 |

### 증상
`onque-frontend`를 처음 Vercel에 배포하려 하자 배포 자체가 진행되지 않고 다음과 같은 취약점 경고로 차단됐다.

```
Vulnerable version of Next.js detected, please update immediately.
```

### 재현 조건
- 환경: `onque-frontend/package.json`의 `next` 버전 `16.0.3`
- 재현 절차: `npx vercel --prod --yes`
- 재현율: 항상

### 원인
- **표면**: Vercel이 배포 파이프라인에서 취약점 스캔에 걸려 배포를 거부.
- **근본**: Next.js 16.0.3에는 critical RCE(GHSA-9qr9-h5gf-34mp, CVSS 10) 등 다수의 알려진 취약점이 있었다. Vercel은 알려진 심각한 CVE가 있는 Next.js 버전의 배포를 자동으로 막는다.
- **확인 방법**: `npm audit`으로 `next` 패키지 관련 취약점 목록과 심각도를 직접 확인.

### 시도했지만 안 된 것
| 시도 | 결과 | 왜 안 됐는가 |
|---|---|---|
| 경고를 무시하고 재배포 시도 | 동일하게 차단 | Vercel 플랫폼 레벨의 하드 블록이라 애플리케이션 코드 변경 없이는 우회 불가 |

### 해결
`npm install next@16.3.0`으로 업그레이드. `npm audit` 재실행으로 `next` 관련 취약점 항목이 사라진 것을 확인(테스트/개발 의존성 관련 낮은 심각도 항목 7건은 이번 스코프 밖으로 남김).

### 검증
업그레이드 후 `npx vercel --prod --yes` 재실행 → 차단 없이 정상 배포 완료, 배포된 URL 정상 접속 확인.

### 추후 관리
- **재발 방지**: 신규 프로젝트를 `create-next-app`으로 만들 때 항상 최신 안정 버전인지 확인 후 시작하는 습관 필요.
- **모니터링**: 없음. 정기적인 `npm audit` 점검이 후속 작업으로 남음.
- **남은 리스크**: 남겨둔 7건의 낮은 심각도 취약점(개발 의존성 관련)은 아직 미해결.
- **후속 작업**: (선택) 나머지 `npm audit` 경고 정리.

### 배운 점
배포 플랫폼(Vercel)이 애플리케이션 프레임워크 자체의 CVE를 감시해 배포를 막아주는 것은 유용한 안전망이지만, 반대로 말하면 "새 프로젝트를 만들었다"는 사실만으로 최신·안전한 버전이 보장되지는 않는다는 뜻이기도 하다. 배포 전에는 습관적으로 `npm audit`을 먼저 돌려보는 게 싸다.

### 참고
- GHSA-9qr9-h5gf-34mp (Next.js RCE)

---

## TS-003 · Google이 신규 발급한 Auth key(`AQ.` 형식)가 구버전 `google-generativeai` SDK와 비호환

| | |
|---|---|
| **날짜** | 2026-08-04 |
| **영역** | 외부API |
| **심각도** | High |
| **상태** | 해결됨 |
| **소요 시간** | 도구 호출 다수 (원인 오판 포함) |

### 증상
TS-002 보안 사고로 API 키를 재발급받아 `.env`와 Render에 반영했는데도, 배포된 백엔드가 통화 요약 요청마다 500 에러를 반환했다.

```
{"detail":"Gemini 요약 중 오류: <HttpError 400 when requesting
https://generativelanguage.googleapis.com/$discovery/rest?version=v1beta&key=<REDACTED>
returned \"API key not valid. Please pass a valid API key.\". Details:
\"[{'@type': 'type.googleapis.com/google.rpc.ErrorInfo', 'reason': 'API_KEY_INVALID',
'domain': 'googleapis.com', 'metadata': {'service': 'generativelanguage.googleapis.com'}},
{'@type': 'type.googleapis.com/google.rpc.LocalizedMessage', 'locale': 'en-US',
'message': 'API key not valid. Please pass a valid API key.'}]\">"}
```

### 재현 조건
- 환경: `main.py`, `gemini_test.py`가 구버전 `google-generativeai==0.8.5` SDK(`import google.generativeai as genai`, `genai.configure()`, `genai.GenerativeModel()`) 사용, 재발급받은 키가 `AQ.`로 시작하는 신형식
- 재현 절차: `/summarize-call`에 파일 업로드
- 재현율: 항상 (새 키로는 항상, SDK를 바꾸지 않는 한)

### 원인
- **표면**: "API key not valid"라는 메시지만 보면 키 자체가 잘못됐다고 오판하기 쉬움. 실제로 처음엔 "Gemini 키는 항상 `AIzaSy...`로 시작한다"고 잘못 판단해, 사용자가 붙여넣은 키가 잘못된 곳에서 복사한 게 아니냐고 되물었다(사용자는 공식 발급 페이지 `aistudio.google.com/apikey`에서 정확히 복사했다고 확인).
- **근본**: Google이 2026년부터 신규 발급 Gemini API 키의 형식을 `AIzaSy...`(레거시 "Standard key")에서 `AQ....`(신형식 "Auth key")로 전환했다. 레거시 SDK(`google-generativeai`)는 이 신형식 키에 대한 discovery 요청(`$discovery/rest?version=v1beta`) 처리 방식이 새 키 형식과 맞지 않아 "API key not valid"를 반환한다 — 키 자체는 유효했다.
- **확인 방법**: 처음엔 "Gemini 키는 항상 AIzaSy로 시작한다"는 (틀린) 사전 지식으로 판단했다가, 웹 검색으로 Google의 2026년 키 형식 전환 공지를 확인하고 정정. 신규 SDK(`google-genai`)로 마이그레이션 후 같은 키로 정상 응답을 받아 "키는 문제없었고 SDK가 문제였다"를 최종 확인.

### 시도했지만 안 된 것
| 시도 | 결과 | 왜 안 됐는가 |
|---|---|---|
| 사용자에게 키를 다시 발급받거나 다른 경로로 복사했는지 확인 요청 | 사용자가 공식 페이지에서 정확히 복사했다고 확인 | 애초에 키는 문제가 아니었음 — 잘못된 가설로 시간을 소모 |

### 해결
`main.py`, `gemini_test.py`를 레거시 `google-generativeai` SDK에서 신규 공식 SDK `google-genai`(`google.genai.Client`)로 마이그레이션.

```python
# 수정 전
import google.generativeai as genai
genai.configure(api_key=GOOGLE_API_KEY)
model = genai.GenerativeModel("gemini-2.5-flash")

# 수정 후
from google import genai
client = genai.Client(api_key=GOOGLE_API_KEY)
uploaded_file = client.files.upload(file=temp_path)
response = client.models.generate_content(model="gemini-2.5-flash", contents=[uploaded_file, prompt])
```

`requirements.txt`도 `google-generativeai` → `google-genai==2.16.0`으로 교체.

### 검증
로컬 venv에 새 의존성 설치 후 실제 로컬 서버 기동 → 재발급받은 키로 `/summarize-call` 호출해 정상 요약 응답 확인. 이후 Render 재배포 후에도 동일 확인.

### 추후 관리
- **재발 방지**: 없음(SDK 마이그레이션으로 근본 해결).
- **모니터링**: 없음.
- **남은 리스크**: 없음.
- **후속 작업**: 없음.

### 배운 점
"API key not valid"라는 에러 메시지는 문자 그대로 "키가 틀렸다"만 의미하지 않는다 — 키 형식은 맞지만 그 키를 다루는 클라이언트(SDK) 쪽이 새 형식을 지원하지 못해도 같은 메시지가 나올 수 있다. 특히 "항상 이런 형식이다"라는 사전 지식은 서비스 제공자가 형식을 바꾸는 순간 바로 틀린 전제가 될 수 있으므로, 확신을 갖고 사용자를 의심하기 전에 먼저 "그 사이 서비스 쪽 사양이 바뀌지 않았는지"를 검색해봐야 한다.

### 참고
- Google Gemini API 키 형식 전환 공지 (2026년, Standard key → Auth key)
- google-genai 공식 SDK: <https://github.com/googleapis/python-genai>

---

## TS-002 · `gemini_test.py`에 하드코딩된 실제 Gemini API 키가 첫 커밋으로 공개 저장소에 노출 (보안 사고)

| | |
|---|---|
| **날짜** | 2026-08-04 |
| **영역** | Auth/Infra |
| **심각도** | Critical |
| **상태** | 해결됨 (키 폐기·재발급 완료, git 히스토리 정리는 사용자 판단으로 보류) |
| **소요 시간** | 즉시 대응 (발견 즉시 조치) |

### 증상
OnQue 저장소를 `github.com/jjssspark/OnQue`에 처음 push한 뒤, Google이 유출된 키를 자동 탐지해 경고를 보냈다("Your API key was reported as leaked"). 원인을 추적해보니 실험 스크립트 `gemini_test.py`에 실제 Gemini API 키가 문자열로 그대로 박혀 있었고, 이 파일이 저장소의 **첫 커밋**에 포함돼 공개 저장소 히스토리에 영구히 남은 상태였다.

```python
# gemini_test.py (수정 전)
GOOGLE_API_KEY = "<REDACTED — 실제 유출된 키, 이후 폐기·재발급됨>"
```

### 재현 조건
- 환경: 로컬에만 있던 `gemini_test.py`(실험용 스크립트)를 별도 `.gitignore` 처리 없이 그대로 `git add` → 첫 커밋 → `git push`
- 재현 절차: 저장소를 처음 공개로 push하는 시점에 발생
- 재현율: 100% (커밋에 포함된 순간 확정적으로 유출)

### 원인
- **표면**: Google이 "API 키가 유출됐다"는 자동 알림을 보냄.
- **근본**: 실험 스크립트를 빠르게 작성하면서 `.env`를 거치지 않고 API 키 문자열을 코드에 직접 박아넣었고, 이후 이 프로젝트를 git 저장소로 초기화(`git init`)해 GitHub에 처음 올릴 때 이 파일을 `.gitignore`로 제외하지 않아 그대로 포함됐다.
- **확인 방법**: Google의 유출 키 자동 탐지 알림과, 코드 검토로 `gemini_test.py`에서 하드코딩된 키 문자열을 직접 확인.

### 시도했지만 안 된 것
| 시도 | 결과 | 왜 안 됐는가 |
|---|---|---|
| (없음 — 발견 즉시 표준 대응 절차대로 진행) | — | — |

### 해결
1. **즉시**: `gemini_test.py`를 `.env`에서 키를 읽도록 수정(하드코딩 제거).
   ```python
   # 수정 후
   from dotenv import load_dotenv
   load_dotenv()
   GOOGLE_API_KEY = os.getenv("GOOGLE_API_KEY")
   if not GOOGLE_API_KEY:
       raise RuntimeError("GOOGLE_API_KEY 환경변수가 설정되어 있지 않습니다.")
   ```
2. 사용자에게 `https://aistudio.google.com/apikey`에서 **키 폐기 및 재발급**을 직접 진행하도록 안내(채팅에 새 키를 붙여넣지 말 것을 명시적으로 요청).
3. 사용자가 재발급한 새 키를 로컬 `.env`와 Render 환경변수에 반영(값은 한 번도 채팅에 노출되지 않음).
4. git 히스토리(첫 커밋)에 남은 옛 키 문자열은 `git filter-repo` 등으로 지울 수 있다고 안내했으나, 사용자가 "지금은 보류"를 명시적으로 선택해 **미실행 상태로 남김**.

### 검증
새로 발급된 키로 로컬 서버를 기동해 정상 응답 확인(단, 이 과정에서 신형식 키와 구버전 SDK 비호환 문제가 새로 드러남 — TS-003 참고). 옛 키는 폐기됐으므로 설령 히스토리에 남아 있어도 더 이상 유효하지 않음을 확인.

### 추후 관리
- **재발 방지**: `env-config.md` 규약(비밀값은 코드에 절대 넣지 않는다, `.env.example`만 커밋)을 이후 모든 신규 스크립트에 적용. 이번 사고로 배운 점을 계기로 `.env.example` 파일을 프로젝트에 새로 추가.
- **모니터링**: Google API 키 유출 자동 탐지에 의존(이번에 실제로 작동함을 확인).
- **남은 리스크**: git 히스토리 첫 커밋에는 여전히 폐기된 옛 키 문자열이 남아 있다. 키 자체는 무효화됐으므로 직접적 악용 위험은 없지만, 저장소를 보는 사람에게 "한때 실수가 있었다"는 흔적은 남아 있다.
- **후속 작업**: 사용자가 원하면 `git filter-repo` + force push로 히스토리에서 완전히 제거 가능 — 명시적 요청 시에만 진행하기로 합의.

### 배운 점
공개 저장소에 처음 `push`하는 순간은 "실험용으로 대충 쓴 코드"가 전부 영구 기록으로 남는 시점이다. 실험 스크립트라도 API 키를 다루는 코드는 프로덕션 코드와 동일한 기준(`.env` 경유)으로 작성해야 하며, 특히 **첫 커밋**은 나중에 지우기 훨씬 번거로우므로(히스토리 재작성 필요) `git init` 직후 `.gitignore`부터 작성하고 `git status`로 무엇이 포함되는지 반드시 확인한 뒤에 첫 커밋을 만들어야 한다.

### 참고
- `env-config.md` "유출 대응" 절차: 키 즉시 폐기·재발급 → 새 키 반영 → 그 다음 히스토리 정리

---

## TS-001 · `requirements.txt`에 무관한 패키지 63개가 그대로 남아있어 Render pip 설치가 `ResolutionImpossible`로 실패

| | |
|---|---|
| **날짜** | 2026-08-04 |
| **영역** | Infra/Build |
| **심각도** | High |
| **상태** | 해결됨 |
| **소요 시간** | 약 20분 |

### 증상
Render에 백엔드를 처음 배포하자 빌드 단계에서 실패했다. 사용자가 보고한 에러:

```
ERROR: ResolutionImpossible: for help visit
https://pip.pypa.io/en/latest/topics/dependency-resolution/#dealing-with-dependency-conflicts
```

### 재현 조건
- 환경: `requirements.txt`가 로컬 개발 환경에서 `pip freeze`로 그대로 뽑아낸 63개 패키지 목록 (Flask, moviepy, openai, imageio 등 이 프로젝트와 무관한 패키지 다수 포함)
- 재현 절차: Render Blueprint(`render.yaml`)의 `buildCommand: pip install -r requirements.txt` 실행
- 재현율: 항상

### 원인
- **표면**: pip가 의존성 조합을 풀 수 없다고 보고.
- **근본**: `requirements.txt`가 이 프로젝트(`main.py`)가 실제로 import하는 패키지만 담은 게 아니라, 로컬 개발 환경 전체의 `pip freeze` 결과를 그대로 옮겨 담고 있었다. 서로 다른 프로젝트에서 쓰던 무관한 패키지 63개가 뒤섞이면서 그 중 일부의 버전 요구사항이 서로 충돌해, Render의 격리된 빌드 환경에서 pip가 전체 조합을 만족하는 설치 계획을 찾지 못했다.
- **확인 방법**: `grep -E "^(import|from) " main.py`로 실제 import하는 최상위 패키지를 확인 → `fastapi`, `uvicorn`, `dotenv`, `google.generativeai`(당시), `multipart` 5개뿐임을 확인. `requirements.txt`의 63개 항목과 명백히 불일치.

### 시도했지만 안 된 것
| 시도 | 결과 | 왜 안 됐는가 |
|---|---|---|
| 에러 메시지의 pip 공식 문서 링크만 참고해 개별 패키지 버전을 조정 | 비효율적, 근본 해결 아님 | 애초에 이 프로젝트에 필요 없는 패키지들끼리의 충돌이라, 버전을 조정하는 것보다 무관한 패키지를 빼는 게 맞는 방향이었음 |

### 해결
`main.py`의 실제 import 목록만 기준으로 `requirements.txt`를 5개 패키지로 정리.

```
# 수정 전: pip freeze 결과 63개 패키지 (Flask, moviepy, openai, imageio 등 포함)
# 수정 후:
fastapi==0.121.2
uvicorn==0.38.0
python-dotenv==1.2.1
google-generativeai==0.8.5   # 이후 TS-003에서 google-genai로 교체
python-multipart==0.0.20
```

로컬 스크래치 디렉터리에 빈 venv를 새로 만들어 `pip install -r requirements.txt`가 깨끗하게 성공하는지 push 전에 먼저 검증.

### 검증
Render 재배포 트리거 → 빌드 로그에서 `ResolutionImpossible` 없이 설치 성공, 서비스가 정상 기동해 헬스체크(`/`) 응답 확인.

### 추후 관리
- **재발 방지**: 의존성 파일은 항상 "실제 import 기준"으로 유지 — 이후 SQLAlchemy/psycopg 등을 추가할 때도 이 원칙을 유지해 필요한 패키지만 추가함(TS-009 관련).
- **모니터링**: 없음.
- **남은 리스크**: 없음.
- **후속 작업**: 없음.

### 배운 점
`pip freeze > requirements.txt`는 "지금 이 가상환경에 뭐가 깔려 있는가"의 스냅샷일 뿐, "이 프로젝트가 실제로 필요로 하는 것"과는 다르다. 여러 프로젝트를 같은 로컬 환경(또는 같은 가상환경)에서 작업하고 있었다면 특히, 배포 전에는 항상 소스 코드의 import 문을 기준으로 의존성 목록을 다시 좁혀야 한다. 격리된 빌드 환경(Render, CI 등)은 로컬처럼 "어쩌다 보니 되는" 상태를 봐주지 않는다.

### 참고
- pip 의존성 해결 문서: <https://pip.pypa.io/en/latest/topics/dependency-resolution/>
